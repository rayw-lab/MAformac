# Active lessons — 检索入口

本页是非权威短索引，不复制 lessons 历史、不承载当前状态。先按任务选择关键词，再到 `docs/lessons-learned.md`、decision、receipt 或 live source 中读取原文并复核 freshness。

| 任务形态 | 优先搜索词 |
|---|---|
| authority / closure / receipt | `exact subject`、`stale basis`、`byte identity`、`producer self-report` |
| OpenSpec / contract | `agree before build`、`archive`、`machine source`、`generated` |
| 并发与 agent | `writable paths`、`stop-line`、`file-first`、`scope creep` |
| Git / fan-in / push | `dirty path`、`remote readback`、`non-force`、`detached` |
| 测试与 proof | `deliberate red`、`fail closed`、`proof class`、`runtime readback` |
| 文档与 handoff | `CURRENT router`、`predecessor`、`supersedes`、`closeout` |

使用方式：`rg -n '<关键词>' docs/lessons-learned.md docs/commander-log/decisions.md docs/handoffs`。本页不要求随每条 lesson 更新；只有检索主题失效时才调整。

> 🔗 successor C live 检索入口（2026-07-18 起生效）：run root = `/Users/wanglei/Projects/agent-tmux-stack-research/runs/2026-07-17-s8-s9-successor-c-longrun/`；秘书 taskbook = `SECRETARY-C-TASKBOOK.md`、checkpoint = `SUCCESSOR-C-CHECKPOINT.md`；post-terminal runbook = `SUCCESSOR-C-POST-TERMINAL-RUNBOOK.md`。本页只维护一条检索指针，不在本页重写 runbook/lessons 正文。
