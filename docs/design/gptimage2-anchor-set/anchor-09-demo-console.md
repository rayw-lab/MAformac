# 锚点 A09 — 演绎控制台（米白日间 · iPhone 竖屏）

> **冷启动提示词文件** · 人工网页版 gpt-image-2 · **上传 A01 iOS 基线图**作风格基准。
> **承接 grill**：SD14 演绎控制台布局（iPhone 控制中心式竖排模块卡）/ SD15 视觉对齐 10 族卡高级感 + segmented 互斥 / SD13 端状态三大块（A 整车 / B 座舱 / C 环境）/ SD8 方案经理幕后工具。
> **主要页面**（磊哥点名要）。挖掘点：gptPRO 没画控制台。

---

## 0. 操作说明

1. 新会话 → 「进阶专业」→ 「创建图片」→ **上传 `anchor-01-idle-baseline.png`**（保持米白玻璃风格）。
2. 粘贴第 2 节。比例 **9:16**，单张。存 `anchor-09-demo-console.png`。

---

## 1. 角色与任务设定

```
你是资深 iOS 26 HMI 设计师。以上传图为视觉风格基准（米白、Liquid Glass、SF Pro 字体），生成「演绎控制台」页面——方案经理的幕后配置工具，iPhone 控制中心式竖排模块卡 + segmented 分段控件。视觉要对齐主页 10 族卡的高级感，不能 too low。
```

---

## 2. 完整提示词（粘贴这一整段）

```
以上传图的米白 Liquid Glass 风格为视觉基准（配色、玻璃质感、圆角、字体一致），生成一张 iPhone 竖屏满幅 UI 截图：车机助手「MAformac」的「演绎控制台」页面（方案经理幕后工具），米白日间，iPhone 控制中心式竖排模块卡布局。

【顶部】标题「演绎控制台」+ 右侧关闭按钮。

【竖排四个圆角 material 模块卡，自上而下】
1. 常态运行卡：一行「● 当前常态」状态点 + 文字；下方两个按钮——「查看全部」（带网格图标）与「⟲ 一键复位常态」。
2. 整车运行卡：标题「整车运行」；「时速」一行 segmented 分段控件 [静态 | 泊车 | 城市 | 高速]（城市高亮选中）；「挡位」一行 segmented [P | R | N | D]（D 高亮选中）。
3. 环境情境卡：标题「环境情境」；「天气」segmented [晴天 | 雨天]（晴天选中）；「时段」segmented [白天 | 夜晚]（白天选中）；互斥单选。
4. 座舱场景卡：标题「座舱场景」；一排胶囊标签场景宏 [上车迎宾 | 离车收尾 | 雨天关窗 | 我有点困了 | +]。

【风格】模块卡 iOS 26 Liquid Glass material 质感 + 柔和阴影 + 圆角 22pt；segmented 分段控件用标准 iOS picker 样式（选中项高亮胶囊）；SF Pro 清晰字号层级；8pt 栅格留白；视觉对齐主页 10 族卡的高级感；暖白底 #F5F3F0；深色文字 #1A1A1A；0.5pt 极浅描边无黑框。

【光照与边缘氛围】柔和日间光；屏幕四边极轻青色氛围内发光。

【硬性约束】满幅 UI 正视；禁手机壳、禁桌面背景、禁人物、禁拼贴；中文准确清晰；segmented 高亮选中项要明确；单张；比例 9:16。
```

---

## 3. 精确规格表

| 模块卡 | 内容 |
|---|---|
| 常态运行 | ●当前常态 + [查看全部] + [⟲复位] |
| 整车运行 | 时速[静态\|泊车\|城市\|高速] + 挡位[P\|R\|N\|D] |
| 环境情境 | 天气[晴天\|雨天] + 时段[白天\|夜晚] 互斥 |
| 座舱场景 | 场景宏胶囊[迎宾\|收尾\|关窗\|困了\|+] |

---

## 4. 关键编码点 + 验收

- **iPhone 控制中心式竖排模块卡**（SD14，非 PPT 三列长条）。
- **segmented 互斥**：选中项高亮胶囊（时速/挡位/天气/时段）。
- **视觉对齐 10 族卡高级感**（SD15，不 too low）。
- **三大块**：整车（时速/挡位）+ 环境（天气/时段）+ 座舱（常态/场景宏）（SD13）。
- **5-gate**：模块层级、segmented 清晰、字号层级、高级感。
