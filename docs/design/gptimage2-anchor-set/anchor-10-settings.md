# 锚点 A10 — 设置面板（米白日间 · iPhone 竖屏）

> **冷启动提示词文件** · 人工网页版 gpt-image-2 · **上传 A01 iOS 基线图**作风格基准。
> **承接 grill**：SD8 右上角设置 = 方案经理幕后配置工具（主题切换 / 场景宏 force / demo 配置）/ 客户不碰、轻量好用不上演绎台面。
> **主要页面**。挖掘点：gptPRO 没画设置。

---

## 0. 操作说明

1. 新会话 → 「进阶专业」→ 「创建图片」→ **上传 `anchor-01-idle-baseline.png`**（保持米白风格）。
2. 粘贴第 2 节。比例 **9:16**，单张。存 `anchor-10-settings.png`。

---

## 1. 角色与任务设定

```
你是资深 iOS 26 HMI 设计师。以上传图为视觉风格基准（米白、SF Pro），生成「设置」页面——方案经理幕后配置工具，标准 iOS 设置分组列表风格。重点是简洁专业好用（幕后工具不上台面），不要花哨。
```

---

## 2. 完整提示词（粘贴这一整段）

```
以上传图的米白风格为视觉基准（配色、圆角、字体一致），生成一张 iPhone 竖屏满幅 UI 截图：车机助手「MAformac」的「设置」页面（方案经理幕后配置工具），米白日间，标准 iOS 26 设置分组列表风格。

【顶部】标题「设置」+ 右侧关闭按钮。

【分组卡片列表，自上而下】
1. 外观分组：一行「主题」+ 右侧 segmented 分段控件 [米白 | 深空]（米白高亮选中），下方一行浅灰副标题「现场光线实时切换」。
2. 演示分组：标题「场景宏」；三个可点列表项「上车迎宾」「雨天关窗」「我有点困了」，每项左侧一枚触发图标、右侧一个 › 箭头。
3. 配置分组：「双端标识」一行 + 右侧开关（开）；「语音音量」一行 + 右侧滑块（中间位置）；「语言」一行 + 右侧值「中文」+ › 箭头。

【风格】iOS 26 设置列表的 material 圆角分组卡 + 组内 hairline 浅分隔线 + SF Pro 字号层级；简洁专业不花哨；暖白底 #F5F3F0；白卡柔和阴影；深色文字 #1A1A1A；8pt 栅格留白；圆角 16-18pt；开关用标准 iOS 绿色样式。

【光照与边缘氛围】柔和日间光；屏幕四边极轻青色氛围内发光（克制）。

【硬性约束】满幅 UI 正视；禁手机壳、禁桌面背景、禁人物、禁拼贴；中文准确清晰；列表项对齐整齐；单张；比例 9:16。
```

---

## 3. 精确规格表

| 分组 | 项 |
|---|---|
| 外观 | 主题 segmented [米白\|深空] + 副标题 |
| 演示 | 场景宏 [上车迎宾 / 雨天关窗 / 我有点困了] |
| 配置 | 双端标识开关 / 语音音量滑块 / 语言 中文 |

---

## 4. 关键编码点 + 验收

- **幕后工具定位**：简洁专业，客户不碰、不上台面（SD8）。
- **主题切换 segmented**：[米白|深空]（V6 强制色，米白默认）。
- **场景宏 force**：方案经理显式触发入口（SD8/E3）。
- **标准 iOS 设置列表**：分组卡 + hairline 分隔（不自创花哨）。
- **5-gate**：列表对齐、字号层级、简洁、易扫读。
