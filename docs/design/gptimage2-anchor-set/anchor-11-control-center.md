# 锚点 A11 — 控制中心弹窗 · 33 base 全态（米白日间 · iPhone 竖屏）

> **冷启动提示词文件** · 人工网页版 gpt-image-2 · **上传 A01 iOS 基线图**作风格基准。
> **承接 grill**：SD14 常态运行卡「查看全部」→ iPhone 控制中心式底部弹窗 / AllStateSheet（33 base 按 10 族分组网格一次看全）/ SD15 顺序铺开 / 端状态全集 33 base = 31 device + 2 safety。
> **主要页面**。挖掘点：gptPRO 没画；这是「一屏纵览全车端态」的信息密度页。

---

## 0. 操作说明

1. 新会话 → 「进阶专业」→ 「创建图片」→ **上传 `anchor-01-idle-baseline.png`**（保持米白风格）。
2. 粘贴第 2 节。比例 **9:16**，单张。存 `anchor-11-control-center.png`。

---

## 1. 角色与任务设定

```
你是资深 iOS 26 HMI 设计师。以上传图为视觉风格基准（米白、Liquid Glass、SF Pro），生成「查看全部端状态」的 iPhone 控制中心式底部弹窗——33 个端状态 base 值按 10 族分组紧凑网格，一屏纵览全车端态。信息密度高但层级清楚不挤。
```

---

## 2. 完整提示词（粘贴这一整段）

```
以上传图的米白 Liquid Glass 风格为视觉基准（配色、玻璃质感、圆角、字体一致），生成一张 iPhone 竖屏满幅 UI 截图：车机助手「MAformac」的「查看全部端状态」控制中心式底部弹窗 sheet，米白日间，从主页上滑浮现。

【弹窗结构】底部弹窗 sheet 浮在半透明模糊的主页之上；顶部一个小横条 grabber + 标题「全部端状态 · 33 项」。

【内容 — 33 个 base 值按 10 族分组紧凑网格】每个族一个小分组：族名小标题 + 该族各 cell 的值小格，例如：
- 空调：开关 关、温度 24℃、风量 1挡、模式 制冷
- 座椅：加热 0、通风 0、按摩 关、靠背 100%
- 车窗：位置 0%、童锁 关
- 屏幕：亮度 70%
- 氛围：颜色 关、亮度 0
- 音量：等级 30%、静音 否、模式 媒体
- 车门：中控锁 锁、儿童锁 关、后备箱 关
- 雨刮：开关 关、速度 0、模式 关
- 天窗遮阳：位置 0%
- 香氛：开关 关、模式 森林
每个值用小字 + 浅灰底白格的紧凑卡片格呈现。

【风格】iPhone 控制中心般的圆角分组卡 + iOS 26 Liquid Glass 半透明 material + 柔和阴影；8pt 栅格密排但不拥挤；SF Pro 小字清晰；信息密度高、层级清楚；暖白底 #F5F3F0；深色文字 #1A1A1A；圆角 16-18pt；0.5pt 极浅描边无黑框；弹窗顶部两角圆角。

【光照与边缘氛围】柔和日间光；弹窗与底层主页之间柔和过渡，屏幕边缘极轻氛围内发光。

【硬性约束】满幅 UI 正视；禁手机壳、禁桌面背景、禁人物、禁拼贴；中文数值准确清晰、不挤成一团；单张；比例 9:16。
```

---

## 3. 精确规格表

| 分组（10 族）| cell 值示例 |
|---|---|
| 空调 | 关 / 24℃ / 1挡 / 制冷 |
| 座椅 | 加热0 / 通风0 / 按摩关 / 靠背100% |
| 车窗 | 0% / 童锁关 |
| 屏幕·氛围·音量·车门·雨刮·天窗·香氛 | 见正文 |
| 合计 | 33 base = 31 device + 2 safety |

> 数值取 state-cells `default` 一手（常态运行初值）。

---

## 4. 关键编码点 + 验收

- **控制中心式弹窗**：底部 sheet + grabber + 半透明模糊背景（SD14 AllStateSheet）。
- **33 base 分组网格**：按 10 族分组一次看全（摘要卡只显主 cell，这里看全集）。
- **信息密度高但不挤**：8pt 栅格密排 + 层级清楚（aesthetic gate2）。
- **数值准确**：取 state-cells default 一手（别编值）。
- **5-gate**：分组层级、对齐、字号可读、不挤成一团。
