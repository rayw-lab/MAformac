# 锚点 A05 — R2 安全拒识 unsafe（米白日间 · iPhone 竖屏）

> **冷启动提示词文件** · 人工网页版 gpt-image-2 · **上传 A01 iOS 基线图**。
> **承接 grill**：SD19 场景2 / scene5 R2 安全拒识必演 / CC-B1（后备箱 unsafe 主值切被拒 cell）/ CC-B2 行驶态纯话术（vehicle.* 不 render）/ D7 unsafe 红克制 / E8-C orb think→speak。
> **挖掘点**：gptPRO 图没拒识态，本张补 **unsafe 红**（demo climax 高光）。

---

## 0. 操作说明

1. 新会话 → 「进阶专业」→ 「创建图片」→ **上传 `anchor-01-idle-baseline.png`**。
2. 粘贴第 2 节。比例 **9:16**，单张。存 `anchor-05-r2-refuse.png`。

---

## 1. 角色与任务设定

```
你是资深 iOS 26 车机 HMI 设计师。以上传图为视觉基线，场景改为「行驶中用户说『打开后备箱』，系统安全拒识」。关键：红色要克制温和（优雅拒识不是冷冰冰报错），不画车速数字（靠话术传达行驶中）。
```

---

## 2. 完整提示词（粘贴这一整段）

```
以上传图的米白连续舞台风格为视觉基线（布局、orb、玻璃卡、字体一致），生成一张 iPhone 竖屏满幅 UI 截图：车机助手「MAformac」主页，米白日间，行驶中用户要求开后备箱被安全拒识。

【orb】青紫渐变球刚从 think 思考态切到 speak 态（带星点微粒子），体现"想了一下再回答"。

【对话区】右侧浅蓝紫气泡「打开后备箱」；左侧白气泡「行驶中为了安全暂时不能开门，停稳后我再帮您」，语气温和。

【车门族卡 — unsafe 安全态，本张焦点】这张卡处于安全拒识态：边缘一圈柔和的警示红描边（红色面积克制、不刺眼）；卡内一枚盾牌 shield 图标；主值临时显示「后备箱 · 行驶中锁定」（不是平时的中控锁状态）。整体是「优雅地挡了一下」而非报错。

【其余族卡】保持正常米白静默灰、半透明淡显，不被红色干扰。

【底部 mic dock】「按住说话」玻璃胶囊 + 左蓝点 + 右声波。

【风格】iOS 26 Liquid Glass；暖白底 #F5F3F0；白卡柔和阴影；深色文字；8pt 栅格留白；圆角 22/16-18pt；0.5pt 极浅描边无黑框；无分割黑线。

【光照与边缘氛围】柔和日间光；屏幕四边极轻氛围内发光（保持中性，不渲染成警报红屏）。

【硬性约束】满幅 UI 正视；不显示任何车速数字（安全语境只靠话术）；红色面积克制；禁手机壳、禁桌面背景、禁人物、禁拼贴；中文准确清晰；单张；比例 9:16。
```

---

## 3. 精确规格表

| 元素 | 规格 |
|---|---|
| unsafe 色 | 柔和警示红 `#FF5C6C`（唯一该用红的态，克制）|
| unsafe 图标 | 盾牌 `exclamationmark.shield.fill` |
| 车门主值 | 临时显「后备箱·行驶中锁定」（CC-B1：非平时中控锁）|
| 行驶态 | **不画车速数字**（CC-B2 纯话术守红线）|
| 话术 | 温和「停稳后我再帮您」（优雅非报错）|

---

## 4. 关键编码点 + 验收

- **unsafe 红克制**：小面积红描边 + 盾牌，不是警报红屏（D7：优雅拒识是 demo 卖点）。
- **CC-B1 主值切换**：后备箱被拒（tailgate），但车门卡平时主值是中控锁 → 临时切显「后备箱·行驶中锁定」（SD19 元洞察）。
- **CC-B2 行驶态**：不 render vehicle.speed 数字，靠「行驶中」话术（守红线）。
- **orb think→speak**：体现安全思考演出（E8-C 1.0s）。
- **5-gate**：红色面积克制、话术温和、不吓人。
