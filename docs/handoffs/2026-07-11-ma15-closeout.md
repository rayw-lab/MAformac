# ma15 收官交接（2026-07-11 全天，wave-2 + v5b + carrier 弹药日）

> 起手读链：本文 → `docs/CURRENT.md` → v6 roadmap（O1:GENERATED=canonical 计数）→ decisions **D-150~D-152**（+ ma16 若落 D-153）→ `runs/2026-07-11-ma15/`（REPORTS-INDEX v4≈180 行）→ V7 计划（若已 RATIFY）→ MEMORY as-of。  
> tip 锚：`opt/streamline-macos-20260707`——收官终值见本文件所在 commit 的 HEAD（D-153 批次；**≠** `origin/main`）。

## 🐝 ma16 蜂群规格（磊哥令 · 必须同布局）
- 阵容：**3 codex + 2 grok**（共 5 worker）。布局：**commander 左 3/5 宽（上 2/3）+ 其下 1 codex（左列 1/3 高）+ 右列 2/5 宽四等分**（余 4 worker）。配方同 ma14/15：`new-session -d -x 300 -y 80` → `split-window -h -l 40%` → 右列三次 `-v` → 左列 `-v -l 33%`；attach 后 `resize-pane` 校正。pane id≠视觉序，派单前 `tmux list-panes` + capture **亲核 vendor/model**。
- **grok busy 判定**＝输入区 **`[stop]` spinner**（勿仅凭 idle 误派）。
- **哨兵**必挂：`runs/2026-07-11-ma15/scripts/swarm-sentinel.sh`（或 ma16 拷贝），**`run_in_background`** 挂法才会唤醒 commander；消息驱动收稿优先，哨兵兜底。
- 拉满教义继承：worker 不闲；派单文件边界+孙子乘法；**producer≠auditor**；收稿以 run-dir 文件为准。

## 完成账（ma15）
1. **十连 push 链**（post 红门基座）：`2bbc5158→…→83d470af→fbc4574b→c96c6785→cae485eb`（D-152 / classfix / reanchor helper 入 upstream；详 CURRENT）。
2. **两波 decision 全闭**：D-150 G3 波 + D-152 wave-2 **17/17 RATIFIED** + **GOV-001=C**（五类另键原样；proof cap exact-nine 5/2/2）。
3. **收编清单**：G1-T1 / rebuild-c6 accepted / A4-0b 门接线 / W5a·W5d·B1b **DONE**（O1 **done=4/29**）/ classfix+helper（closure checker **53/53** 账）。
4. **v5b ABI**：**四层**（授权→五键 env→sandbox fail-closed→host finalizer）后 **proof PASS**（`V5B-ABI-PROOF-RECEIPT`；contain @≈`59dc8822`+后继；**未**合入 mainline tip=ma16 收编）。
5. **carrier 包 READY 待键**：W7 v2=`T-FIX-CONFIRMED`；W8 v3=`T-FIX-CONFIRMED_CARRIER_READY`；W9+V2 v2=`APPROVE_P2_CLOSED`；呈拍=`CARRIER-KEY-ONEPAGER-v2`；W7-P1 RED 用例表已备。
6. **V7 作战计划** commander 亲笔 **v2**：双审（A=P1×1+P2×5 / B=P1×8+P2×2）**已全折**（`V7-BATTLE-PLAN-DRAFT-commander.md` status=DUAL_AUDIT_ABSORBED）——ma16 呈磊哥 RATIFY 即可。
7. **CI trigger 修复已收编 push**（verify workflow 覆盖本分支；required check 首跑=ma16 观察位）+ docs 批次（CURRENT/lessons **M78-M85**/swarm-runs ma15 段/CLAUDE dedup）入库。

## 未完成 + ma16 第一步
1. **V7 RATIFY**（v2 已折双审）→ **carrier 键** → ⭐序 **W8 propose 写回**→W7→W9-amend→V2（SUPERSEDED_BY_CARRIER 防双 SSOT）。
2. **v5b contain 收编** mainline + waiver supersede 账（registry/决策条目）。
3. ~~D-153~~ **已随本批落库**（v5b branch proof+今日尾账；contain 收编+W2 residual 关闭仍=ma16）。
4. 并行：remote CI trigger（opt 分支 required `verify` 从未跑）；A5-019 ⭐A+020 呈拍；S8 仅 readiness（`NOT_READY_TO_IGNITE`）等 run-auth；GOV 实证轮等 8 门物化后。

## 磊哥键清单
| 键 | 弹药 |
|---|---|
| V7 RATIFY | `V7-BATTLE-PLAN-DRAFT`（v2，双审已折） |
| carrier（三/四 change） | KEY one-pager v2 + W7/W8/W9V2 终 plan |
| A5-019 action | `A5-MANIFEST-FINAL-ONEPAGER` ⭐A retain+020 |
| S8 run-auth + 安静窗 | `S8-READINESS-v2`（仍 NOT_READY） |
| 全仓 GOV 推广 | 仅 EMPIRICS 8/8 后（DESIGN-GOV001） |
| A4-1 等 | 另拍；不阻塞 carrier |

## 关键教训（一行索引）
**M.78–M.85**（lessons v4）+ global **`roster-sync-on-set-expansion`** + **NEVER_GREEN**（授权/env/sandbox 分层；phantom make **48/49**；remote required check 未触发）— 详 `DRAFT-LESSONS-v4` / SWEEP-NEVER-GREEN / V5B-TEST-ICEBERG。

## Non-claims
`actionDemoProven=0/120`；`baseline_activation=PENDING_CASCADE`；S8 **未**点火；wave-2 **无** apply/coding/merge；carrier **未**写 openspec；v5b proof **≠** operator-pass/V-PASS/C6/candidate；V7 **未** RATIFY；GOV 全仓 **未** enforce；mainline tip **未**含 contain ABI 代码。
