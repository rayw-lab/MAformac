# Handoff 2026-06-19 — C1/C2 apply: 整改+复审+T2闭合+纵切验全闭环 → archive 待 demo-scenarios

> ⚠️ **HISTORICAL 快照（T5）—— 文档级联 banner（2026-07-07 B4b）**
> 本文是 `docs/grill-tournament/cascade-inventory.md §T5` 标记的历史快照，当前仅保留溯源/交接价值；supersede 指针以 `docs/grill-tournament/cascade-inventory.md` 的 T5 账本为准。
> **活基线** = `CLAUDE.md §9` + `docs/grill-tournament/grill-decisions-master.md` + `docs/c5-recovery-2026-06-22/grill-decisions-amend-paradigm-tool-surface.md` + `docs/grill-tournament/cascade-inventory.md`。正文保留供溯源，勿据此推进。

## 一句话状态
`define-c1c2-contract` apply **核心闭环**:C1 codegen(3990)+ C2 纵切端态 + GPT Pro 双审整改(verdict **APPROVE 可 archive**)+ L1 allowlist reviewed + **T2 C1↔C2 闭合(76 L1 行)+ 纵切验全栈绿**。**archive 待最后一块 = `demo-scenarios.yaml`(磊哥定 5 幕场景,magnet reviewed)+ risk-policy(CC)**。

## ⚠️ 起手(分支)
v2/T2 全在 **`feat/define-c1c2-contract`**(worktree `~/workspace/MAformac-c1-codegen`,HEAD `6bb9be3`,已 push PR#2);main 仍旧版。`make verify` 全绿(`state_cells=active l1_closure=ok L1_rows=76`),`make verify-generated` source-free 也绿。

## 本 session 完成(海量)
1. **核 codex C1 codegen**(a42dc5a)V-PASS:make verify 实跑 + 辩证 quarantined=0 非洗白(openpyxl 独立计数吻合)+ 脱敏 0 命中。
2. **C2 state-cells 纵切两设备**(f8dded5)+ `verify_state_cells` C2 内部门(四态/枚举拆分 + source_kind 防漂)。
3. **GPT Pro 双审**:首审 REQUEST_CHANGES → CC 整改(6c478f3)+ codex 项 CC 接手(b1badf2,codex 沙箱够不到 worktree)→ 复审 **APPROVE 可 archive**(`docs/handoffs` 旁 `Downloads/pr_audit_2(2).md`)。辩证降级:CI matrix/全套pytest/性能/license/stage.yaml backlog。
4. **L1 allowlist reviewed**(3.2):纵切批 ac_temperature/window + transition_id 一手绑定;横铺批 screen/ambient/safety pending C2。数据从 3990 协议 + 12000 bug 挖(脚本 `raw/05-Projects/MAformac/stageB-mining/`)。
5. **C5/C6 parked design 回写**(62e2729)+ **7-repo adopt 选型研究**(3c5d3f1,7 CC subagent 各 clone+读代码:Hammer/xLAM/unsloth/BFCL/tau2/When2Call/ACEBench;`docs/lora-eval-adopt-research-2026-06-19.md`)。
6. **T2 C1↔C2 闭合**(6bb9be3):gen_c1.apply_l1_allowlist 派生 L1 + execution_range_ref;verify_l1_closure 双向校验;closure deferred→active;纵切空调温度+车窗闭环。

## 下一步(archive 收 S2 → C3-C7 解冻)
1. **磊哥定 `demo-scenarios.yaml`**(4.2,5 幕场景话术→指令绑定,magnet reviewed)← archive 前关键缺口。
2. CC 补 **risk-policy.yaml**(3.1)+ C2 横铺 cell(4.1:screen/ambient,接 allowlist_pending_c2)+ 6.1 supersede(spec delta 已写)+ 6.2 文档级联核 + 7.2 verification。
3. `/opsx:archive define-c1c2-contract` → spec 入库 → **按 `_parked/README` 复用度逐个 rebase C3-C7**(C7 voice🟢/C3 execution🟡骨架46340f1/C4路由·C5 LoRA·C6 bench🔴重写)。
4. C5/C6 rebase 用 7-repo adopt 蓝本 + 3 HIGH(见 memory `maformac-lora-train-eval-stack`)。

## 起手读
CLAUDE §9 → 本 handoff → `docs/lora-eval-adopt-research-2026-06-19.md` → memory(`maformac-lora-train-eval-stack` / `maformac-c1c2-coupled-change-T2` / `maformac-l1-vs-training-scope`)。tasks 13done/7余。
