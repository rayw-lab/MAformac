# UIUE 8.G9b U17 XCUITest + L0 Evidence Receipt

## Conclusion

`8.G9b / U17` is complete for simulator L0 smoke scope: the repository has a real `MAformacIOSUITests` target, a minimum golden path XCUITest, and an on-screen `simctl io screenshot` L0 evidence package.

This receipt does not close `8.C2`, does not claim L3, and does not claim `V-PASS`.

## Evidence

- L0 manifest: `l0-evidence.json`
- UI tree evidence: `u17-ui-tree.txt`
- On-screen screenshot: `u17-golden-path-simctl.png`

The XCUITest result bundle and xcodebuild log are transient run artifacts generated by the capture harness and cleaned after a successful run; they are not required L0 screenshot evidence.

## Tool / Official Doc Absorption

- Adopted `xcodebuild test` with a real Xcode UI test target and `XCUIApplication.launchArguments` for the `-goldenPathID` entry contract.
- Adopted accessibility identifiers plus the XCUITest UI tree for `ui_tree_evidence`.
- Adopted `xcrun simctl io booted screenshot` for on-screen L0 screenshot capture.
- Did not use Preview, `ImageRenderer`, static snapshots, or XCUITest attachments as L0 screenshot evidence.
- Did not make XcodeBuildMCP or `serve-sim` a required path for this harness because the active MCP defaults pointed at another worktree; the committed harness stays CLI-reproducible in this worktree.

## Re-run

```bash
cd /Users/wanglei/workspace/MAformac-uiue
Tools/checks/capture-u17-l0-evidence.sh
python3 Tools/checks/check-u17-l0-evidence.py docs/research/2026-06-27-uiue-8g9b-u17-l0
```

## Proof Class

- `simulator_l0_runtime_truth`
- Supporting proof: local/unit/XCUITest

## Claims Not Made

- No mobile proof
- No true-device proof
- No L3 human 5-gate verdict
- No V-PASS
- No A-2 complete claim
- No real NLU/ASR/TTS/LoRA/backend readiness
