> ⚠️ **PARTIAL — 基于 6/18 路成功**（L01/L02/L03/L05/L06/L15；其余 12 路 rate-limited 待补跑后重综合）。本报告结论不完整，仅供过渡参考。

---

## Stop-the-Train 风险矩阵（核心交付物：别返工，不是知道更多）

> 表头：| 风险 | 触发条件 | 提前发现手段 | stop-the-train 阈值 | owner | 需磊哥拍板 |
> 排序：能否提前阻止 0/34 复发（P0 → P1 → P2）。每行的「提前发现手段」是落进 OpenSpec gate task 的弹药，不是泛泛风险。

| # | 风险 | 触发条件 | 提前发现手段 | stop-the-train 阈值 | owner | 需磊哥拍板 |
|---|---|---|---|---|---|---|
| **R1** | **中途无行为门，通宵跑完才暴露 0/34**（codex 自主跑/自审无外部门，GOV6 根因） | retrain-c5 用 mlx-lm 原生 val-loss 门 / 无中途 generate 抽样 / 门在 agent 执行上下文内 | iter50/100/150 密 checkpoint 抽样 generate→解析 toolCall→C6 第一/二层各 N≥5 分层覆盖 4 类×多族；grep 门实现禁止只读 val_loss；单测喂 toolCalls=[] 全塌 fixture 门必须 FAIL | golden 抽样 0/5 或 fuzz < base 相对门 → **early_stop**；边界值（如 2/5）→ **human_pause** 让人拍 | 训练脚本/CI（infrastructure-enforced，非 codex 自审）；retrain-c5 owner | **是（C14 阈值/四态/谁有权停）** |
| **R2** | **train/eval/runtime tool surface 异源**（0/34 真根因之一，generic frame vs D-domain） | 中途门 generate 用了 generic frame `tool_call_frame` 而非 A2 ToolContractCompiler D-domain 产物 / eval surface ≠ runtime surface | 单测断言「门 generate surface == C6 eval surface == runtime surface」三者同一 ToolContractCompiler 产物（TRN2/AUD5 同源 enforce） | 三方 surface hash diff ≠ 0 → **blocked**（candidate UNSIGNED 不放行） | A2 ToolContractCompiler 消费方 + retrain-c5 owner | 否（A2 已修方向，落实装验证 task） |
| **R3** | **chat-template byte-parity 失守**（训练 assistant 段带 think 块、端侧默认不带 = 端侧少 4 prompt token，offset 漂） | 端侧 mlx-swift 加载非 patched tokenizer（issue #154）/ render_parity_diff 未真接端侧 / D-domain 工具名前后引号空格不同源 | `C5EndpointTokenizerParityGate`（`C5LoRATraining.swift:1612`）真接 mlx-swift endpoint render 逐字节 UTF-8 比 + think 签名 + 比 mask offset 起点 token（非只比 prompt 字符串）+ 多轮历史一致性 | endpointBytes ≠ trainingBytes 或 endpointRendered=nil → **blocked**（lessons #26 candidate BLOCKER） | retrain-c5（gate 契约）+ define-demo-golden-run-and-voice（端侧 render 源，DEFERRED） | 否（gate 契约写 task；端侧实装延后） |
| **R4** | **byte-offset masking off-by-one**（transformers #34462 砍尾换行 + D-domain 多 token 工具名 → assistant 边界学偏一 token，loss 不一定异常但端侧隐蔽塌，比 loss-mask 更难 catch） | 引入多轮样本未拆单轮 / 自定义 per-turn mask 把非 assistant token 置零（mlx 实证 val_loss=NaN）/ fixture expected_start 未随 D-domain 更新 | `c5_mask_offset_fixture.py` 对真 MLX tokenizer dump trained_span 首 token=`<tool_call>`/NO_TOOL 且不含 user/system/think marker；多轮必拆单轮样本；生产前 print 一条 decode trained-span 肉眼核 | 任一样本 >1 assistant 轮且未拆 → **block**；trained_span 含 user/system marker → **block** | retrain-c5 owner | 否（写进 retrain-c5 数据 gate task） |
| **R5** | **enable_thinking 渲染不一致**（样本若带 `<think>...</think>` 而 fixture 期望不含 → 验证器持续红，根因在数据生成不在 mask） | 样本生成未用 enable_thinking=False 渲染 / Qwen3 默认带 thinking | fixture:104 断言 trained span 不含 `<think>`/`</think>`；样本生成显式 enable_thinking=False | trained_span_contains_think=true → **block**（修数据非修 mask） | retrain-c5 数据生成 owner | 否（数据 gate task） |
| **R6** | **LR 回到 2e-4 / 未守 repo-loop clip → loss 尖刺发散**（iter70-80 实测 spike 17-32；home-llm 用 2e-4 易被照搬） | config LR≠1e-4 / 走 stock mlx_lm.lora（无 clip）而非 c5_mlx_train_loop.py / 照搬 home-llm Axolotl 2e-4 | 训练前 grep config 确认 LR=1e-4+cosine+warmup0.08；确认走 repo-loop（有 clip_grad_norm 1.0）；10-iter preflight 看 val | val 非 finite / loss spike > 阈值 → **blocked**（NonFiniteTrainingError 守护已在 `c5_mlx_train_loop.py:324`） | retrain-c5 owner（配方冻结） | 否（C16 冻结 rank16Mainline，已 Phase 0 接受） |
| **R7** | **约束解码掩盖语义塌缩**（grammar 强制必吐合法 tool_call，把 0/34 空数组变成合法但选错工具，更隐蔽「看似在工作」） | 约束 grammar 不含拒识/no-op/unsupported 合法分支 / 只约束到必调用 | grammar 必含拒识/clarify/unsupported 合法出口；C6 加「被迫合法但语义错」轴（约束 vs 无约束 action 正确率对比） | 约束解码下 action 正确率 << 无约束下 / 拒识被阉割 → **human_pause** | define-demo-golden-run-and-voice（受限解码 vendor，DEFERRED） | **是（约束解码 vs 安全拒识张力，C20 已接受 GBNF fallback only）** |
| **R8** | **8GB iPhone jetsam**（1.7B-4bit + KV cache 多轮 DialogueState + 竞争 app → per-process 上限设备相关被打爆 kill；所有 benchmark 在 12GB 设备） | 拿 iPhone17Pro 12GB 数据当 8GB 可行性证据（用别的环境数据冒充，硬约束红线） | 8GB 真机跑 os_proc_available_memory() 实测上限（非 Mac 推断）；模拟竞争 app 压力下跑 3 轮 DialogueState；KV cache 上限+滑窗 | 真机 available_memory < 模型+KV+runtime 总和 → **blocked**（不依赖 increased-memory-limit entitlement，App Store 构建常不生效） | define-demo-golden-run-and-voice（端侧 spike，DEFERRED） | 否（C19 端侧证据边界已接受，禁端侧 ready 声称） |
| **R9** | **100% 合成数据 model collapse**（>30% 自由 LLM 合成降 8-15%；MAformac 若数据全合成） | 用自由 LLM 直接产 jsonl 而非「LLM 填种子→确定性 templating 展开」/ 无真实座舱 3990 种子锚 | 采 home-llm 两阶段（synthesize.py 填种子 + generate_data.py 确定性展开）；种子锚 3990 协议契约 SSOT；异源 judge + 契约定标签 + 原文 oracle 非训练集 | 训练集 0 条自然中文 / 全自由 LLM 生成无确定性 → **human_pause**（C5 superaudit 北极星缺口） | retrain-c5 数据生成 owner | **是（C12 template-heavy vs cloud-NL 配比，hypothesis 待 spike）** |
| **R10** | **末轮 checkpoint 非最优**（Instruct-SkillMix 实证 loss 上升而 held-out 峰在 mid-training；默认取末轮会取到退化点） | early_stop / 训练结束默认取末轮 checkpoint | 存「best-behavioral-pass-rate checkpoint」而非末轮；receipt 记 best_checkpoint_step + best_behavioral_metric（ALTO patience 机制） | 末轮行为 pass-rate < best-checkpoint → 取 best 而非末轮 | retrain-c5 owner | 否（写进 C14/C18 checkpoint 选取 task） |
| **R11** | **门本身假绿成第11坑**（门 receipt 写 behavioral_gate=PASS 是 metadata 声称；门 generate/parse 有 bug 或读 flag 而非实跑 → 假绿） | 门 parse 永远返回非空 / 读 readback flag 而非实跑 output / 信 flag | 门 generate 必须实跑模型 + 实解析 + value-in-source 核（解析出的 toolCall 真在生成文本里）；recompute 内核 fail-closed + 异源 grader（hermes 非同 Claude 家族） | grader 挂 → candidate UNSIGNED 不降级签（sign-or-block） | CI/harness owner | 否（harness enforce 层已上线，扩到行为门） |
| **R12** | **训练栈跑不动**（显存/吞吐/OOM） | M5/32GB 训 1.7B-4bit | 本机历史受票据已坐实 peak 11.4-12.2GB/no_oom；grad-checkpoint 现成逃生口（省 30-60% activation） | peak 逼近 32GB → 降 seq/--grad-checkpoint | retrain-c5 owner | 否（**paper-tiger，硬件从来不是瓶颈，标 P2**） |