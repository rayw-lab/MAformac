---
authority: grill_in_progress_skeleton_upfront
series: F044-R2
status: open_2026-07-03_night（D-046 范式；弹药=三 worker 在途稿 + 今晚 W6/W7 新稿 + 反思档）
decision_ref: D-079（FAIL verdict）/ D-080（今晚编排）
basis: F044-VERDICT.md + f044-fail-baseline-reflection-2026-07-03.md（矛盾监督根因/评测偏斜/门缺格三坐实）
---

# F044 Round-2 grill（wave-2 配方 × 数据修复 × 评测扩建 × 再短训评）

> 前置事实（反思档坐实）：A 轴反转根因=**矛盾监督**（同协议串 28:16 双标签，表示层丢极性）；ac 族内极性配比 open 反占优=「配比失衡」假设已反证；eval A/B 轴=纯 ac 族偏斜；DataGate 缺监督一致性门。

| ID | 问题 | 弹药源 | 状态 |
|---|---|---|---|
| R2-1 | 🔴 矛盾监督全量清剿与修复方案 | W6 扫描+teardown / %43 T1 / commander 亲核锚例 | **locked（commander 拍=重渲染治本）**：W6 全量扫描 329 组/波及 686 行（train 269 组+dev_selection 60 组=val 也被污染，解释 val loss 假漂亮）；teardown file:line 定位=**C1 契约无罪**（`contracts/semantic-function-contract.jsonl:17/21` open_mode/close_mode 分明，seed 已 decode `C5LoRATraining.swift:237-238`），丢失在 **`renderUserUtterance` :1853**（user 只渲染 device/primitive/slots/value，intent/action_code 压扁；assistant 侧 :2667 用 seed.intent=信息不对称）；329 组全 `NO_UNIQUE_LABEL_UNDER_CURRENT_RENDERING`=两侧皆合法能力→**清洗选侧=阉割能力面，否决**（%43 T1「无 source-backed winner 就停」同判）→ 拍=**重渲染**：user 串插 `action=<action_code>` 段+mount seeded shuffle 同刀（T3）+全量重渲染+扫描器 0 矛盾证明；A 轴 case 同格式重产（旧 A 锚失效，R2 用 paired base 当场重建），B/D 锚不动。执行=%44（渲染器）+%61（A case 工具）。 |
| R2-2 | 监督一致性门常设 | W6 + %44 | **locked**：%44 已实装 `tools/supervision_consistency_scanner.py`（key=归一化输入+state+mount 集+safety class+slot defaults+split+basis，codex 修正版）；今晚作独立前置门（重渲染后必 0 矛盾/显式 allowlist），R3 进 C5DataGate 主路径常设格（W6 已定位 DataGate 缺口 `C5DataGate.swift:275-301` 无此格；仓内 `C5CandidateDataQualityGate` :663 有局部思路未挡 substrate）。 |
| R2-3 | 负例配比：query 37 行（0.8%）极度稀疏非绝对真空；query/refusal/already_state/unsupported 四类的 expected 形态（query_* 工具 vs no_call，从 C6/契约推导）与各族目标行数 | %44 gen-harden 稿 + %45 recipe 稿 | **locked（D-082 B+ 方案）**：负例批拍定不入 R2a 单变量窗口（premortem T2/T8：~40 行负例在 2.2 epoch 改 qa 行为期望低）；%45 六件套已备 R2b。兑现：R2 verdict MP-029 按预告仍 FAIL（安全级）→ R2b 必修主靶点②（query_ac_temperature 强负例+硬评测） |
| R2-4 | 近邻族分离：set_interface/defog、airoutlet、raise-lower-little vs adjust_to 的数据侧分离规则；%43 的 D 轴形态靶点表映射到配比 | %43 crosscheck 稿 + %45 | **open→R2b 主靶点①（弹药已齐）**：R2 verdict 坐实残余失败全部=近邻混淆（A 残余 5+B 全 6，per-case 表见 F044-R2-VERDICT.md）；三层升维判（D-084）：L1 表示维度未丢（W6 区分度审计 `/Users/wanglei/Projects/agent-tmux-stack-research/runs/2026-07-03-n2n4-train-readiness/W6-DATA-LEARNABILITY-AUDIT.md:139-140`：interface/defog 由 device/primitive 可见区分、airoutlet/wind 由 device 可分）→ 纯 L2 配方问题（同档 :149-150 覆盖读数：interface_vs_defog 104 样本/6 unique label、airoutlet_vs_wind_direction 46/5=对比信号稀薄）；同配方多训被否（无对比信号可学，固化错误） |
| R2-5 | eval bundle 扩建：A/B 轴 10 族化（每族协议+自然对）；D 轴负例 case 补充；扩建后 v6 锚失效→**新 base 锚必须同 harness 同 scorer 重测**（锚绑 scorer 口径，M.17） | W7 | **open（设计✅执行挂 R3）**：W7-EVAL-BUNDLE-V2-DESIGN.md 已交；R2a 按拍走原 bundle 保锚（B/D base 锚 9/15、18/34 精确复现=口径自证） |
| R2-6 | F-044 阈值口径分形态：A 15/15 底线源自 tiny 死记，泛化训练是否同阈（口径型→default 拍或磊哥）；D 锚随 bundle 扩建重定 | commander 拍 | **locked（R2a 已按此判；R2b 门磊哥已拍 D-085「全部同意」）**：R2a 放行线=A≥12/15 且 open→close 反转=0 且 D≥18/34 不退化 且 qa=0（D-080 五条之①）；实判 A 10/15+qa 1 → F044_R2_FAIL。**R2b 门锁定（D-085）**：A≥12 + **B>9/15（zero delta 即 FAIL）** + D≥18 + qa=0 + **极性 open→close/close→open 双向单列**。A 轴 15/15 底线终裁=只适用 tiny 死记场景，泛化短训评 A 门=12/15 |
| R2-7 | R2 短训评规模与预算：wave-2 corpus 目标行数（负例+极性修复+近邻分离）；训练时长（本次实测 3h54m 锚）；今晚窗口能否闭环 round 2 | %45 recipe 稿 + commander | **locked（实测收口）**：R2a=B+ 方案 4750 行同规模单变量；实测 13,138s≈3h39m（23:18 起跑→02:57 落盘，指挥官会话中途掉登录、nohup+watchdog 无人值守跑完）/峰值 17.974GB/150 updates 满額；eval fresh process ~9min |
| R2-8 | A/B base 数字与 v6 差异注脚 | %43 crosscheck 稿 | **locked（注脚性质，不阻塞）**：crosscheck F4 已记 base 行为差异成因（base 常输出裸 JSON 无 `<tool_call>` wrapper 被记 empty=scorer wrapper 敏感；D 锚 18/34 精确复现证明 D 面可比）；🔴 叠加 R2-1 重渲染后 **A 轴 input 分布变更，base A 值将再漂移——R2 的 A 轴解读只用 paired delta（同输入 base vs adapter），禁引用 round1 A base 3/15 或 v6 A base 0/15 作跨轮比较**（%43 oracle 段1 在补 prior art 纪律）。 |
| R2-9 | openspec carrier 与 substrate 语义审计债 | %45 WD-12 建议 | **locked（记录制，今晚不动 openspec）**：R2 全程走 run-dir receipt+plan carrier；若 F044 成为长期硬门→R3 开窄 change `define-f044-shorttrain-behavior-gate`（%45 建议采纳）；重渲染改的是训练渲染器代码（A2 D-domain 范式内表示层修正）不动 C1 契约=无需 C 系列 change（磊哥授权在手，但 W6 teardown 证明契约无罪，不动是正确用权）；substrate 语义审计债（4250 行未过现行 judge）——重渲染+0 矛盾证明+新格式 judge 抽样部分补账，全量补账 carrier 挂 R3。 |
| R2-10 | R2 成功判据与终止条件：round 2 仍 FAIL 怎么办（第三轮 vs 回 C1 契约层 vs 换配方范式）——预落决策树同 WD-10 范式 | commander 拍（弹药齐后） | **locked（预拍已执行）**：D-080 预拍=FAIL→停训转 R3（渲染重构+bundle v2）不连训第三轮；R2 verdict=F044_R2_FAIL 分层（D-084）→ 已按条款执行：不起第三轮、不起正式训练（条件①不满足）、转 R2b 聚焦两靶点（近邻 contrastive + 负行为面）。回 C1 契约层被否（W6 teardown 证契约无罪）；换配方范式无据（A+7/D 收复=方向有效） |

## 消减记录
（弹药回填后逐条 locked；每拍一条同步回写状态列）
- 2026-07-04 晨（D-084 级联）：R2-3/6/7/10 locked、R2-4 转 R2b 主靶点①、R2-5 设计✅挂 R3——R2 系列 10 题全部有终态或明确 R2b/R3 归属，本 grill 骨架收口。R2b 门 proposal（A≥12+B>9+D≥18+qa=0+双向极性单列）挂磊哥确认后开 R2b grill 新骨架。verdict 三源复核一致（commander+%43 AFFIRM+%61 recount MATCH）。
