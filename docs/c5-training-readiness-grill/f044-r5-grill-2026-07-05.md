---
authority: grill_in_progress_skeleton_upfront
series: F044-R5
status: skeleton_partially_superseded_by_D108_B_and_formal_launch_host_hold
created: 2026-07-05
decision_ref: D-104
basis:
  - docs/commander-log/decisions.md:D-104
  - /Users/wanglei/Projects/agent-tmux-stack-research/runs/2026-07-03-n2n4-train-readiness/R5-STRATEGY-REDTEAM.md
  - /Users/wanglei/Projects/agent-tmux-stack-research/runs/2026-07-03-n2n4-train-readiness/R5-QNEG-DIFF-ONEHAND.md
proof_class: local_static_doc_skeleton
non_claims:
  - no R5 decision locked
  - no data generation authorized
  - no training authorized
  - no runtime implementation authorized
  - no D-085 qa=0 hard-gate relaxation authorized
---

# F044 R5 qa 修复决策 grill（骨架 upfront，2026-07-05）

> **2026-07-05 晚补记**：本 skeleton 的 R5-2/R5-3 议题已被 D-108/D-110 后续路线部分改写：Phase 4 已拍 B（runtime-gated qa safety，formal evidence-run path 解锁，禁写 adapter learned qa），但 candidate 仍 unsigned。formal 1800 当前不是被 R5 skeleton 阻塞，而是被 launch-adjacent host gate 阻塞：run-auth accepted / command v2 clear / watchdog v2 clear / host HOLD / not launched。R5 pair-boundary 仍可作为 falsification/数据路线讨论，不得被写成 formal PASS 或 candidate signoff。

本文件只搭 R5 决策 grill 骨架。所有议题先放 `default_locked_placeholder` 占位，等 %2 机理、%3 策略红队、%5 一手归因回稿后再填弹药、消减和 landing；本文不替 worker 归因，不替磊哥拍口径型决策。

入口事实锚：

- D-104：R4 verdict=`F044_R4_FAIL_QA_REGRESSION`；R4 后 A 15/15、B 15/15、D 21/34，A/B/D 连续两轮（R3/R4）稳定 PASS；qa 跨轨 `total=12 / adapter=10 / base=2 / original_v3=0`，R3 adapter=8 到 R4 adapter=10 恶化（`docs/commander-log/decisions.md:774-780`）。
- D-104：T1 失败精确定性为工具名幻觉/近邻混淆，非 over-refusal；观察到不存在的 `query_volume` / `query_fragrance_mode` / `close_door`，对应真名存在且训练行挂载是真名（`docs/commander-log/decisions.md:777`）。
- D-104：mount 同构化作为 R3 到 R4 唯一变量被证伪；候选范式改为“同 mount 同 device 的问句/操作对比对”，但仍需对抗归因确认（`docs/commander-log/decisions.md:778-779`）。
- D-104：qa 三轮数据手段未破（9 -> 8 -> 10），策略选择上抛磊哥：R5 对比对数据 / runtime 兜底加 A/B/D 满分候选先晋级 / 双管（`docs/commander-log/decisions.md:780`）。

## 0. 议题矩阵（upfront）

| ID | 议题 | 待填弹药源 | default_locked 占位 | 待填判定表头 | landing / 出口 |
| --- | --- | --- | --- | --- | --- |
| R5-1 | 对比对范式是否成立：同 mount、同 device 下，“问 X -> NO_TOOL” vs “操作 X -> 调工具”紧邻成对，是否才是 qa 判定面 | %2 机理归因；%5 一手 diff；D-104 mount 同构证伪 | `default_locked_placeholder`；不默认成立 | 成立证据 / 反证 / 适用 family / 成对最小构造 / pair ledger 要求 | 若成立，进入 R5 数据配方；若不成立，停止数据-only 路线或仅保留 falsification experiment |
| R5-2 | runtime 兜底边界：unsupported 族 query 在路由层是否不挂 mutating tools；true query 与 action-question 如何分流 | %3 策略红队；W55 P1 runtime mount narrowing；W50/W51 live-loop 接线事实 | `default_locked_placeholder`；不默认替代 D-085 | unsupported query / true query / action-question 三分规则；proof class；是否改 gate semantics | 若采纳，需独立 OpenSpec/receipt；禁写成 adapter learned qa |
| R5-3 | qa=0 硬门 vs A/B/D 三轴满分候选先晋级 | D-085 放行线；D-104 A/B/D 连续满分事实；%3 策略红队 | `LEIGE_KEY_REQUIRED`；worker 不拍 | 维持 qa=0 / runtime safety gate 可替代 / 双轨推进 / formal 1800 影响 | 只能由磊哥/总监裁决；未裁决前不得把候选晋级写成默认 |
| R5-4 | R5 配方：iters、负例配比、对比对构造，同款仅改数据 | R5-1 结论；%5 一手行级差异；R3/R4 train/eval receipt | `default_locked_placeholder`；不默认继续 600 iters | iters / rank16-LR 是否逐字节不动 / 负例:正例:guard 比例 / nn_group 成对 / true query guard / action-question guard | 若进入生成单，必须先出数据守恒、pair completeness、DataGate、judge、strict preflight |
| R5-5 | 停线：第 N 轮、数据手段到头、换策略触发 | %2/%3 归因；D-103/R4 stopline 执行结果；R5-1/R5-2 选择 | `default_locked_placeholder`；N 不预填 | qa 未清零 / T1 退化 / A-B-D 回退 / tool-name hallucination 扩散 / host 风险 | 任一 P0 停线触发时，不临场拍 R6；转入 runtime/架构/基座策略 grill |

## 1. R5-1 对比对范式（待填）

| 栏位 | 内容 |
| --- | --- |
| 问题 | 同 mount、同 device、同 noun 下，模型是否必须看到“问状态”和“发操作”的最小可辨对比，才能学会 qa no-call 边界？ |
| 支持证据 | `<FILL_FROM_%2_%5>` |
| 反证 | `<FILL_FROM_%2_%3_%5>` |
| 最小构造 | `<FILL>`：unsupported status-question -> `NO_TOOL`；same noun imperative/action -> mutating tool；true query family -> `query_*`；action-question control -> mutating tool。 |
| 必须守住 | expected tool 必须在 mount 内；真 query 五族不误伤；工具名真名精度不退；pair ledger 必须能区分 standalone guard 与 paired contrast。 |
| 默认占位 | `default_locked_placeholder`: R5-1 未锁。 |

## 2. R5-2 runtime 兜底边界（待填）

| 栏位 | 内容 |
| --- | --- |
| 问题 | unsupported query 族是否应在 route/mount 层先被识别为不挂 mutating tools，而不是继续让 adapter 在大工具面里自判不出手？ |
| 支持证据 | `<FILL_FROM_%3_W55_P1>` |
| 反证 | `<FILL_FROM_%2_%3>` |
| 边界草案 | unsupported query -> no mutating mount / unsupported readback；true query -> only `query_*` or query route；action-question -> 保留 mutating mount。 |
| 必须守住 | runtime 兜底若采纳，proof class 是 `runtime-gated qa safety`，不能写成 `adapter learned qa`；当前 runtime 接线成熟度需另证。 |
| 默认占位 | `default_locked_placeholder`: R5-2 未锁。 |

## 3. R5-3 qa=0 硬门 vs 候选晋级（磊哥键）

| 栏位 | 内容 |
| --- | --- |
| 问题 | 在 A/B/D 连续两轮满分/过门但 qa 三轮数据手段未破的情况下，D-085 的 `qa=0` 是否仍必须由 adapter-only eval 满足，还是允许 runtime safety gate 满足后候选先晋级？ |
| 选项 A | 维持 D-085：adapter-only qa=0 仍是硬门；R5 必须先破 qa。 |
| 选项 B | 拆门：A/B/D 作为 model candidate gate，qa 交给 runtime safety gate；需正式修订 D-085 口径。 |
| 选项 C | 双管：R5 对比对做一次 falsification experiment，同时启动 runtime guard 兜底；晋级条件另锁。 |
| 必须守住 | worker 不拍；任何“先晋级”都必须写明 proof class 和 non-claims。 |
| 默认占位 | `LEIGE_KEY_REQUIRED`: 只留决策格，不给推荐默认。 |

## 4. R5-4 配方（待填）

| 栏位 | 内容 |
| --- | --- |
| 配方边界 | 同款仅改数据；iters/rank/LR/watchdog/host baseline 是否逐字节沿 R3/R4，待 R5-1/R5-3 后确认。 |
| 数据面待填 | family 覆盖、每族 pair 数、negative:positive:guard 比例、nn_group 命名、D-087 真 query guard、action-question control。 |
| 门前检查 | pair completeness；contradiction scanner；semantic-function contract 真名核；DataGate；judge；strict preflight；host baseline。 |
| 训练后检查 | qa cross-track；T1 true-query/action-question；A/B/D；expanded B/Q；tool-name hallucination scan。 |
| 默认占位 | `default_locked_placeholder`: 不授权生成、不授权起训。 |

## 5. R5-5 停线（待填）

| 栏位 | 内容 |
| --- | --- |
| 第 N 轮 | `<LEIGE_KEY_OR_ATTRIBUTION_FILL>`；不预填 N。 |
| 数据手段到头触发 | qa 未到 0；T1 真 query/action-question 退化；工具名幻觉扩散；A/B/D 回退；同一解释重复失败。 |
| 换策略触发 | R5-1 被证伪；R5 对比对仍失败；runtime 兜底被证明能更直接满足 demo safety；host/memory 风险压过短训收益。 |
| 禁止动作 | 不把“再堆量/再换 mount/再加 epoch”作为默认 R6；不把 R5 partial 改写成 formal PASS。 |
| 默认占位 | `default_locked_placeholder`: 停线未锁。 |

## 6. 弹药入口

| 来源 | 当前角色 | 落点 | 状态 |
| --- | --- | --- | --- |
| `%2` 机理归因 | 解释 R4 失败机制，核对“对比对”是否是真判定面 | R5-1 / R5-4 / R5-5 | `<PENDING_INTAKE>` |
| `%3` 策略红队 | 评估 runtime 兜底、gate semantics、数据-only stopline | R5-2 / R5-3 / R5-5 | `<PENDING_INTAKE>` |
| `%5` 一手 | R3/R4 qneg diff、mount/expected/nn_group 实况 | R5-1 / R5-4 | `<PENDING_INTAKE>` |
| D-104 | 最新决策事实锚：R4 fail、A/B/D pass、qa 9->8->10、工具名幻觉 | 全部议题 | `READ_IN` |

## 7. 消减记录

| 时间 | 议题 | 新证据 | 消减 | 状态 |
| --- | --- | --- | --- | --- |
| 2026-07-05 skeleton | R5-1..R5-5 | 仅 D-104 入口事实；归因回稿待填 | 只建表头，不消减 | `OPEN` |
| 2026-07-05 晚 | R5-2/R5-3/formal launch 关系 | D-108=B；W-G2 command v2 clear；W-H2 watchdog v2 clear；W-I/W-I2/W-I3 host HOLD；W-J host NO-GO audit | R5 runtime/gate semantics 不再是 formal evidence-run 当前阻塞；active blocker 转为 host PASS/waiver + runtime armed proof。R5 不签 candidate，不替代 C6/V-PASS | `PARTIALLY_SUPERSEDED_FOR_FORMAL_LAUNCH` |

## 8. Landing 守门

- `R5-1` 未锁前，不发 R5 数据生成单。
- `R5-2` 若采纳，必须单独落 OpenSpec/receipt，并标 `runtime-gated qa safety`，不能冒充 adapter-only qa 过门。
- `R5-3` 是磊哥键；未裁决前，任何候选晋级、D-085 松动、formal 1800 启动都不得由 worker 写成默认。
- 2026-07-05 晚：D-108 已给 formal evidence-run 口径 B，但 **candidate 晋级仍未签**；formal launch 仍不得绕过 host PASS/waiver、watchdog armed、first-real LR。
- `R5-4` 若进入执行，必须先列完整门：组装守恒、DataGate、judge、strict preflight、host baseline、watchdog。
- `R5-5` 若触发停线，先开策略/架构 grill，不临场拍 R6 数据手段。
