# F044 R3 修复轮 grill（骨架 upfront，2026-07-04 夜连开；D-046 范式+红队成对）

> **2026-07-05 晚 supersession**：本文件的 R3 formal 1800 framing 已成为历史前置。当前 formal launch 入口是 W-G2 command v2 + W-H2 watchdog v2 + fresh host PASS/waiver + high executor；run-auth 已接受，但 host gate 仍 HOLD，formal 1800 未 launch。R3 仍是 recipe/basis 历史锚，不是当前 launch-ready receipt。

> 溯源：D-095 终判 F044_R2B_FAIL_STRATIFIED → R3 数据订单（①9 具名族 unsupported-query 强负例 ②interface/airoutlet 近邻×协议记忆组合样本 ③B 轴不动）。目标=修 qa 跨轨 9 违例（D-097 后唯一真靶点；原「A 轴 5 残余组合面」premise 作废=量尺病），D/近邻单轮面 25/26 不回退。
> 授权口径：数据面作业（生成/judge/门/组装）=commander 职权内连夜推进；**R3 短训起跑=条件式**（全门绿+host 双阈值+watchdog armed 才起，任一不绿=hold 至磊哥晨键）；**正式训练起跑=必等 R3 过门+磊哥晨键**。

| # | 议题 | 状态 | 锁定 |
|---|---|---|---|
| R3-1 | 负例 cap：R2b envelope no-call ~10% cap 是 qa 恶化根因之一。R3 目标负例 ≈9 族×10 变体=~90 行，加入后 no-call 占比升至多少、cap 是否显式上调 | default_locked | **cap 上调为「负例目标行全保留，不下采样」**，占比实算入 receipt（约束物化 M.21：写进 assembler 参数非 prose）；理由=per-family 覆盖是本轮靶点，不能被 cap 采掉 |
| R3-2 | 9 族负例形态：每族问句变体→ NO_TOOL；正例对照成对防矫枉过正 | **locked+AMMO-2 佐证** | 量级按 R3-AMMO-2 定：**108 负例（12/族）+36 对照（4/族）**；根因坐实=R2b 训练集 unsupported-query 严格口径仅 8 行（broad 13）→9 族问句面近零监督；加入后 no-call 占比 2.97% 无需动 cap（R3-1 的 cap 上调条款自然满足）；D-087 五 query intent 族问句仍映射 query_*（每族≥2 防误伤行）。证据=R3-AMMO-2-QA-REGRESSION.md |
| R3-3 | 组合样本形态（原议题） | 🔴**PARKED per D-097**（premise 作废：A 轴 5 残余=eval case mount-invalid 非组合泛化缺失；52 行留盘不并包） | ≥40 行组合样本：interface open/close×三区/airoutlet 全组合 + 记忆语境变体；nn_group 与单轮 25/26 已修行错开（防重复监督冲突，过 supervision scanner） |
| R3-4 | 配方：同 R2b knobs（rank16/LR1e-4/600 iters/8192）仅数据面追加 | default_locked | 同款不动（W30 config 体系可复用；R3 短训=600 iters 150 updates 原 schedule 非 formal 450） |
| R3-5 | 门与锚：R2b locked floors 单调不回退 + 本轮新增两靶点专项门（qa 跨轨=0 硬线/A≥12/近邻单轮面≥24/26 不回退/D≥18） | default_locked | 放行线沿 D-085 + 新增「近邻单轮不回退」；判 PASS 上限=两新靶点修复证明，不扩大 |
| R3-6 | 🔑磊哥键（晨）：R3 过门后正式训练 1800 iters 起跑授权 | reserved | 晨键；Launch Packet 六件复用，仅 verdict/host-baseline 换绑 R3 |
