# MAformac 吸收内化措施 — 优化待讨论清单（磊哥慢慢看）

> **用途**：三次深扒（home-llm runtime / home-llm data / Mastra / Pi）→ MAformac 的吸收内化措施总清单 backlog。磊哥逐项审视，可在「讨论」列批注 ✓采纳 / ✗砍 / 改。每项标来源(file:line) + 落点(change) + 动作 + 状态。
> **🔴 元认知（磊哥 2026-06-20 定）**：**star > 1000 的 repo 默认不降级吸收**——能 1000 star 是有原因的，工程形态多半被生产验证过。已回流 `~/.claude/rules/blueprint-teardown.md`。本清单按此原则：工程价值一条不砍，只 drop 真不适用的载体（分布式基础设施/Node runtime/与红线冲突的自由 agent loop/重治理 ceremony）。
> **源**：`docs/research/2026-06-19-home-llm-teardown{,-data}.md`、`docs/research/2026-06-20-mastra-teardown-workflow-eval-trace.md`、`docs/research/2026-06-20-pi-teardown-collaboration-layer.md`。
> **star**：home-llm 1365★ / Mastra 25257★ / Pi 64099★（均当天 push，全过 github-first）。

---

## 不降级原则（吸收时扣住）
- ✅ 全量吸收：让小模型/方案可靠的**工程价值/设计形态**——防御解析 / 单位归一化 / 受限解码 / KV 预热 / 确定性编排 / eval 契约 / judge / 数据配方 / session 溯源 / hook 契约。
- ❌ 才 drop（真不适用，非降级）：JS/Node runtime 进 iOS、自由 agent loop（撞红线）、分布式工作流基础设施（evented/pubsub/snapshot）、TS Proxy 魔法、sandbox/隔离（磊哥定不考虑）。

---

## C3 执行（home-llm runtime + Mastra trace）— codex apply 实装中

| # | 措施项 | 源 file:line | 状态 | 讨论 |
|---|---|---|---|---|
| 1 | 单发 MAX_ITER=0 → single-call runtime contract | home-llm conversation.py:156 | spec✅ 实装中 | |
| 2 | 三层防御解析（双 schema + parser-repair 候选，repair 后过 semantic） | home-llm utils.py:495 | spec✅ | |
| 3 | 双向单位归一化（模型经验值/code 按 C2 算机器值，**全字段落地不取第一个 scalar**） | home-llm utils.py:565 | spec✅ | |
| 4 | **受限解码 GBNF**（1.7B 保格式底座，XGrammar 1B schema 22%→100%；MLX outlines/xgrammar 等效待 spike） | home-llm output.gbnf | spike 7.2 **必做非可选** | |
| 5 | **KV 缓存预热**（冷启动解药，启动+状态变化重热；首句快=demo 关键） | home-llm llamacpp:366 | spike 7.4 **必做** | |
| 6 | 三重白名单（domain×service×arg）→ DemoGuard | home-llm const.py | spec✅ | |
| 7 | gate 链 fail-closed（schema→semantic→precondition→stale→repair→guard→execute→readback） | SRD §12.1 | spec✅ | |

## C4 路由（Mastra workflow）— parked，C4 propose 时

| # | 措施项 | 源 | 不降级点 | 讨论 |
|---|---|---|---|---|
| 8 | DemoFlow = `enum Stage` tagged-union 纯数据序列（控制流是值不是代码） | Mastra types.ts:505 | 确定性骨架全量 | |
| 9 | 定义→commit 冻结→执行三相（执行期不改图） | Mastra workflow.ts:2273 | 拒自由 loop 工程兜底 | |
| 10 | 分支由 code condition 判定非模型 + loop 有界+abort+iterationCount | Mastra control-flow.ts:264/568 | 对齐「安全是代码」 | |
| 11 | RunStatus(10态)/StepResult(6态) 全枚举不用布尔 | Mastra types.ts:266 | 对齐「错误用枚举」 | |
| 12 | **Mastra agent loop 失败清单写进 C4 design Risks**（#6827 重复 mutative call / #11273 maxToken 静默杀 loop / context rot 不报错越答越差） | oracle/GitHub issues | 拒自由 loop 最强外部证据 | |

## C5 LoRA 数据（home-llm data + 已锁 stack）— parked，C5 propose 时

| # | 措施项 | 源 | 讨论 |
|---|---|---|---|
| 13 | 5 类样本（templated 多意图 / status 读回 / failure 错误恢复 / refusal 状态拒识 / asr_noisy） | home-llm data teardown | |
| 14 | 模板随机参数泛化 + distractor 落域 | 同 | |
| 15 | train_on_turn masking（失败步不训，下轮 apology/clarify 必训） | 同 | |
| 16 | 配比（templated 最重 10-25x） | 同 | |
| 17 | + Hammer(function masking)/xLAM/GOAT(arg-token masking) + BFCL-v3/tau2/When2Call 评测 | lora-train-eval-stack memory | |
| 18 | 数据三源全用（3990 协议 + 12000 bug + raw 语料） | 同 | |

## C6 评测（Mastra eval）— parked，**最大收益，全量不降级**

| # | 措施项 | 源 file:line | 不降级点 | 讨论 |
|---|---|---|---|---|
| 19 | **`TrajectoryExpectation` 契约整套**（steps+args / `blacklistedTools`=expect_no_call / `noRedundantCalls`=防重复调 / ordering / maxSteps） | Mastra types.ts:550-594 | C6 契约几乎现成，整套抄 | |
| 20 | extractTrajectory 从 trace 抽实际 ToolCall 序列与 expected 比对 | Mastra types.ts:611 | 算法直接用 | |
| 21 | runEvals dataset+pMap 有界并发逐条累加 | Mastra run/index.ts:119 | demo-scenarios→dataset | |
| 22 | ScoreAccumulator 多桶分维 → 全集覆盖率+scenario score 双轴（对齐 D35） | Mastra scorerAccumulator | 双轴 bench | |
| 23 | failure 结构化枚举分类 + scenarioId/scorerId 上下文（never 静默） | Mastra run/index.ts:425 | failure_class | |
| 24 | **scorer 四阶段 pipeline 完整**（preprocess→analyze→generateScore→generateReason，immutable 链式） | Mastra base.ts:377 | **不降级=保留完整形态，给主观维度留位** | |
| 25 | `{score:number, reason:string}` 输出契约固定 | Mastra base.ts:443 | 每 scenario 固定 | |
| 26 | **LLM-as-judge 仅评文本主观项**：judge schema 只 `clarify_text_score / refusal_text_score / reason`，**TTS 删除归人工**；**仅硬门全过后计算，不参与放行硬门、不洗白** | Mastra base.ts:858 | **Q3✅ 磊哥定**：C6 spec 加 `Requirement: judge 不参与放行硬门`；硬门=Unsafe false pass=0/readback mismatch=0/no-tool FP=0/must-pass=100%（vehicle-tool-bench spec:10）；TTS 听感归人（config:48） | |

## C7 语音（home-llm + ASR research）— 多数已锁

| # | 措施项 | 状态 | 讨论 |
|---|---|---|---|
| 27 | sherpa-onnx 中文(Paraformer/SenseVoice)主 + WhisperKit fallback + ASRBackend 抽象（D14 已锁） | 已锁 | |
| 28 | 拼音 fuzzy(CFStringTransform 封闭词表) + LoRA 音近训 + 置信澄清（非热词；端侧不跑 post-ASR LLM 纠错） | 已锁 | |
| 29 | KV 预热/采样 → C7 runtime 接口（与 C3 #4/#5 共享，落点 open question 待定 C3/C7） | open question | |

## Trace（Mastra observability）— C3/C6 共用

| # | 措施项 | 源 | 讨论 |
|---|---|---|---|
| 30 | **C3 五段 `stage{decode,plan,guard,execute,readback}` 不改**（spec:156 + TraceLogger.swift:3 已锁，端到端展示不能动 C3 stage）；`TraceEntry` **补 `runId / parentSpanId / spanKind` 三字段**，C7(ASR)+C4(路由) 产上层 span，C3 五段挂为子树；Mastra=runId 业务串联 + traceId/spanId 树形（**不把 asr/understand/route 塞进 C3 五段**） | Mastra tracing.ts:35/53 | **Q1✅(a) 磊哥定** |
| 31 | BaseSpan schema + runId(业务)/traceId-spanId(OTel) 并存 | Mastra tracing.ts:737 | |
| 32 | currentSpan 透传成树（Swift 显式传，drop TS Proxy） | Mastra context.ts | |
| 33 | span 三态(create/end/error) + internal 位掩码分层（给客户只露五段主干，内部归一化标 internal） | Mastra tracing.ts:1269 | |

## 长任务开发规范（Pi 协作层）— **不降级=现在就立，非"种子待验证"**

| # | 措施项 | 源 file:line | 不降级点 | 讨论 |
|---|---|---|---|---|
| 34 | **事件溯源式 handoff**（append-only 永不回改旧 handoff，当前状态=顺读全部重放） | Pi jsonl-storage.ts:250 | 治本失忆 §28/§29 | |
| 35 | **七段 compaction 硬模板**（Goal/Constraints/Progress(Done/InProgress/Blocked)/KeyDecisions/NextSteps/CriticalContext + 强令留 file:line/报错原文/文件血缘） | Pi compaction.ts:389 | 直接抄成 session-closure 硬模板 | |
| 36 | **工具前后 hook 验收门**（before grep 一手源 block 越界 / after 读回 mock 态校验，「能改什么」类型钉死）→ 派单 schema 验收门 | Pi agent-loop.ts:581 + types.ts:704 | 落地 §5 | |
| 37 | RPC/print headless + get_state「当前状态行」（派单回稿必含 git 分支/测试/make verify 结果） | Pi rpc-mode.ts | 对齐派单第一性 | |
| 38 | steering/follow-up 分级插话纪律（steer=停当前再发 / follow-up=干完这步追加）+ branch_summary 分叉总结回主线 | Pi agent-loop.ts:166 + branch-summarization.ts | 派单插话/worktree 纪律 | |

---

## 跨层一致性硬门（CC grill 补，跨 C3/C5/C6）

| # | 措施项 | 落点 | 状态 |
|---|---|---|---|
| 39 | **Qwen tool-call 格式单一源**：新建 `contracts/qwen-tool-call-format.yaml`（`model_family / runtime_parser=json / thinking=false / wrapper=tool_call / arguments_shape=json_object`）；C3 runtime 锁定，C5 训练样本生成 + C6 bench harness **只引用它**（防 train/runtime/bench 三者 silent 失真：模型学的格式 runtime 收不到 `.toolCall` 漏进 `.chunk`） | C3 runtime + C5 + C6 | **Q2✅ 磊哥定** |
| 40 | replay 可复现：C6 trace 记 `model_id/prompt_hash/seed/tool_output_digest`（否则评测不可复现，无法定位回归） | C6 | 待 grill Q |

## 🔴 之前 CC 倾向降级、现按「star>1000 不降级」提回的 3 项

1. **#26 C6 LLM-as-judge**：原标"drop 默认"→ **提回**。ToolCall/state 用确定性比对，但 TTS 自然度/澄清话术优雅度/拒识友好度是主观维度，保留 judge 评分（带只读工具独立核查形态）。
2. **#24 C6 scorer 四阶段完整 pipeline**：原标"简化"→ **提回**。保留 preprocess→analyze→score→reason 完整形态，给主观维度留位，不预先砍成单函数。
3. **#34-36 Pi 长任务规范 3 形态**：原标"种子待验证"→ **提回**。七段模板/事件溯源 handoff/hook 验收门是被 64k★ 验证的工程，直接立规范，不走"先种子后验证"降级路径。

---

## 状态总览
- **C3（#1-7）**：spec 已定，codex apply 实装中。#4 GBNF / #5 KV 预热 = spike 必做（不降级）。
- **C4（#8-12）**：parked，C4 propose 时按本表 adopt；#12 反面证据进 design Risks。
- **C5（#13-18）**：parked，C5 propose 时按 home-llm data teardown + 已锁 stack。
- **C6（#19-26）**：parked，**最大收益**；#19 TrajectoryExpectation 契约整套抄；#24/#26 judge+pipeline 不降级。
- **C7（#27-29）**：多数已锁（D14）；#29 落点待定。
- **Trace（#30-33）**：C3/C6 共用，强类型 span 体系。
- **长任务规范（#34-38）**：不降级=现在立；#34-36 可优先用于本项目 handoff/派单。

> 磊哥审完批注后，采纳项分发进各 C-change 的 design.md「Research Inputs / adopt 指令」段（像 home-llm teardown 那样 findings→adopt 指令）。

---

## Grill 结论记录（磊哥 × CC，2026-06-20，逐 Q 累积）

> codex 核实 38 项 → `verdict=partial` + 11 项补项/纠偏（全成立）；CC 补 #39/#40（oracle 风险落地）。以下逐 Q grill 拍板：

- **Q1 端到端 trace 分层 → ✅ (a) 磊哥定**：C3 五段 `decode/plan/guard/execute/readback` **不改**（已锁 spec:156 + TraceLogger.swift:3）；端到端 = 一个 `runId` 贯穿全链，C7(ASR)+C4(路由)+C3(五段) 的 span 挂同一 `traceId` 成树，C3 五段是子树。**落点**：`TraceEntry` 补 `runId / parentSpanId / spanKind` 三字段；C7/C4 产上层 span。Mastra 对齐点=runId 业务串联 + traceId/spanId 树形追踪（raw teardown:53），**不是把 asr/understand/route 塞进 C3 五段**。→ 已纠 Trace #30。
- **Q2 #39 Qwen tool-call 格式单一源 → ✅ 磊哥定「加，锁单一源」**：C3 spike 已显式 `toolCallFormat:.json`（spike-e3 main.swift:36）+ `enable_thinking=false`（:96），裸 JSON 掉 `.chunk` 是 9/40 真实格式坑（spike-e3-report:18）。**落点**：新建小文件 `contracts/qwen-tool-call-format.yaml`，字段仅 `model_family / runtime_parser=json / thinking=false / wrapper=tool_call / arguments_shape=json_object`；C5 训练样本生成 + C6 bench harness **只引用它，不各写一套**。C5 parked design:7 已把 LoRA 目标修成稳定 `<tool_call>` 包裹，#39 = 把这从「设计口径」提升成跨 C3/C5/C6 **格式契约**。→ 加措施 #39。
- **Q3 C6 judge 边界 → ✅ 磊哥定「judge 不参与放行硬门 + TTS 删除归人」**：冲突成立（Mastra raw:42 写 judge drop vs 38项 #26 把 TTS/澄清/拒识都提回，自相矛盾）。**落点**：C6 spec 写一条 `Requirement: judge 不参与放行硬门`，硬门沿用 `Unsafe false pass=0 / readback mismatch=0 / no-tool false positive=0 / must-pass=100%`（vehicle-tool-bench spec:10）；judge schema **只留 `clarify_text_score / refusal_text_score / reason`，仅硬门全过后计算**。**TTS 听感归人工**（非自动化信号，config:48 明说听感不混入硬验收）。→ 纠 #26 + 纠 Mastra teardown:42。
- **Q4 C6 四类一等硬门 → ✅ 磊哥定**（含 codex #9 seed schema 化 + #10 硬门）：C6 已写 `no-tool false positive=0`+restraint（vehicle-tool-bench spec:10/:17），design 已写 IrrelAcc 独立一等 + eval ≥20% no-call/无关样本（design:55）。**落点**（按 demo-scenarios:21-23 待办补齐）：case schema 加 `pre_state / input_zh / expected_tool_calls / expect_no_call / expected_state_delta / readback_assertion / clarify_tag / failure_class`；runner 输出 `IrrelAcc / no_tool_false_positive_count / state_delta_match / readback_match / clarify_match`。**任一 no-call 误触发 / 状态差异 / 读回不一致 / 该澄清没澄清 = 硬失败，judge 不参与**。→ 纠 #19/#22/#23 + demo-scenarios C6 字段。
- **Q5 #40 replay 可复现 → ✅ 磊哥定**：落点=C6 `eval_run` 指纹（非只 trace 多字段）。C6 现只写 trace_id/route_kind/parser_status/...（design:37、tasks:6），缺模型/adapter/prompt 指纹。**物理落点**：C6 runner 每条输出 `run_id / case_id / model_id / lora_adapter_id / lora_checkpoint_id / qwen_tool_call_format_version / prompt_hash / sampling_seed / tool_output_digest / contract_digest`，挂 Q1 runId trace 树 → per-checkpoint diff 定位退步。→ 加 #40。
- **Q6 C5 数据门细化（#17/#18）→ ✅ 磊哥定**：C5 已有分桶+must_not_train（design:32/:41）+ masking/负样本方向（:67/:70），但 tasks 仍只写 JSONL 合法/5桶齐/eval-train 无交集（tasks:14/:19）。**物理落点**：C5 生成脚本每批吐 `verification_receipt.json`（`row_count / bucket_counts / format_contract_version / tool_call_format_pass_rate / split_whitelist / parent_semantic_overlap / must_not_train_violations`）；硬门 `parent_overlap=0 / must_not_train_violations=0 / #39 格式合规`。→ 细化 #17/#18。

### codex 11 项核实（CC 逐项核 = 全成立，采纳进讨论列）
1. 源路径纠偏（归位前第5行写 `docs/research/` 但文件尚未入仓，INDEX 也未收录）→ **第一刀已纠**：Mastra/Pi teardown 已归 `docs/research/`，本 backlog 已归 `docs/` 并由 `docs/research/INDEX.md` 收录。
2. Trace 五段不能改（=Q1，已纠 #30）。
3. Trace 强类型 attributes（TraceEntry 只有 message）→ **subagent 已修 gap#6**（candidate_source/tool_call_count/repair_used/guard_reason/readback_result）。
4. C3#1 decode completion 元信息（finish_reason=length 不 repair）→ **subagent 已修 gap#1**。
5. C3#2 参数形态归一（JSONValue 为主，旧 arguments 不参与安全）→ **subagent 已修 gap#2**（实测 arguments 仅 computed property，安全判断走 frame.value/slots）。
6. C3#6 allowlist primitive gate（device+primitive）→ **subagent 已修 gap#3**。
7. C3#7 readback 接 C2 readback_zh 模板 → **subagent 已修 gap#5**。
8. C6 judge 提回对，但写死「确定性门(ToolCall/state/readback)先过、失败总分归零、judge 不洗白、只评主观」→ 纠 #24/#26（待 Q）。
9. C6 seed 正式 schema 化（demo-scenarios 仍 c6_seed_interim，补 input_zh/expected_tool_calls/expected_state_delta/expect_no_call/failure_class）→ #19/#9（待 Q）。
10. C6 no-call / IrrelAcc / 状态型评测列一等硬门（When2Call/ToolSandbox/τ²-bench；只评「调对工具」会假绿）→ 加 C6 硬门（待 Q）。
11. C5 数据质量门拆细（verification_receipt / Hammer masking 函数名·参数名·默认值三形态 / split whitelist / parent overlap=0 / must_not_train=0）→ 细化 #17/#18（待 Q）。

> **C3 的 #3-7（即 gap#1/2/3/5/6）已被 subagent audit-fix 实装并 swift test 46 绿**；#1/#2/#8/#10/#11 是文档/C6/C5 设计补项，待逐 Q 拍板后落各 change。

---

## 落地三刀（磊哥定，按依赖顺序，最少返工）

- **第一刀 归位 + 跨层契约**：纠源路径、teardown 收进 `docs/research/INDEX.md`、新建 `contracts/qwen-tool-call-format.yaml`、#39/#40 + Q1-Q6 写回本清单 + 各 parked change（让它们成可引用合同）。
- **第二刀 C6 优先（不等 C5）**：#19-26 + Q3/Q4/Q5 落 case schema / runner / 四类硬门 / judge 边界 / replay 指纹。理由：没 C6，C5 LoRA 只是「感觉提升」，无法判过拟合/泄漏/no-call 误触发。
- **第三刀 C5 数据门**：#13-18 + Q6。先 receipt / split whitelist / masking 三形态 / must_not_train=0，再扩数据，**不先训练**。
- 旁路：C3 #1-7 已实装线（#4 GBNF / #5 KV 预热单独 spike）；C4 #8-12、C7 #27-29 各自 propose 解冻；Trace #30-33 与 #40 合并成同套 span/run 指纹；长任务 #34-38 现在就落 handoff/派单模板，不等代码。
- **真正要落（codex C3 gap#1/2/3/5/6 已实装别重复打开）**：源路径纠偏 / Q1 trace 分层 / Q3 judge 边界 / Q4 C6 schema·硬门 / Q6 C5 数据门。

### CC 辩证 check（整体强认同 + 3 精化 + 1 纪律）
三刀的依赖序**强对**——C6 优先于 C5 是 LoRA 项目根本纪律（没评测先于训练 = 感觉提升）；数据门先于扩数据；归位先成可引用合同。补 4 点：
1. **C6 优先 = 先 C6 propose（解冻 vehicle-tool-bench → rebase C1/C2 + #39）→ apply；且 C6 第一轮 base Qwen3-1.7B 跑建基线**（lora_adapter_id 空），C5 出 checkpoint 后再 LoRA 跑 diff——这正是「C6 先于 C5」的操作：**base 基线是判 C5 提升/过拟合的判据**。C6 不能直接 apply（parked），要走 propose。
2. **契约纪律**：demo-scenarios 已 archive（C2），Q4 case schema **不直接改它**，由 C6 change 把它从 `c6_seed_interim` 重写成正式 C6 dataset（走 spec delta）；`qwen-tool-call-format.yaml` 新建 standalone（像 risk-policy.yaml）。第一刀「写回 parked change」是记 design/tasks，**不是 apply**。
3. **Q1 trace 三字段**（runId/parentSpanId/spanKind）= C3 `TraceEntry` code 增量，不违五段 stage spec（spec 锁 stage 不限其它字段），可现在补到 C3 apply 分支。
4. **分支纪律**：C3 apply（feat 分支，SOLID 46绿）**先 merge main**，再在 main 做归位/INDEX/qwen-format/写回（避免 teardown/backlog 混进 apply 分支）。
