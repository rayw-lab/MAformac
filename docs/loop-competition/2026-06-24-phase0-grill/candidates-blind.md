# Phase 0 Grill Candidates - Blind Set

| ID | Question |
|---|---|
| C01 | `a2-post-roadmap` should be classified as what kind of artifact: live roadmap, progress SSOT, decision pack, pre-propose checklist, or some other role? |
| C02 | How should the current project split responsibility among the old `roadmap-2026-06-20`, `c5-recovery-2026-06-22/roadmap.md`, `a2-post-roadmap`, OpenSpec changes, and the UIUE branch roadmap? |
| C03 | What exact artifact boundary should distinguish `--scope=full` and `--scope=demo`, and how should the project prove they are derived from one SSOT rather than becoming two SSOTs? |
| C04 | Which archived OpenSpec specs have observable behavior changed by D-domain surface and the four-layer C6 bench, and what should be the disposition for each changed spec? |
| C05 | What Pocock stage should each of A2 follow-up, `retrain-c5`, `rebuild-c6`, endpoint parity, CAS cascade, and G6-C occupy before any implementation work starts? |
| C06 | Which canonical fields or enums should unify SRD route tiers, model-visible surface, runtime outcomes, execution outcomes, readback, unsupported, and safety refusal? |
| C07 | How should D1-D37 and the MASTER semantic protocol be marked as keep, modify, superseded, or deferred after the D-domain paradigm flip? |
| C08 | Which UIUE items must remain outside mainline blockers, and which state or C3-C6 contract intersections must still be kept in mainline Phase 0? |
| C09 | Should MAformac keep four C5 data classes or add an explicit failure/error-recovery class, and what exact artifact should record the decision? |
| C10 | How should MAformac classify `already_state` or state-noop refusals relative to unsupported refusals and safety refusals? |
| C11 | What initial data-class factor ratios should be specified for C5 D-domain training data, and what evidence or spike is required before changing them? |
| C12 | What ratio and contract should govern deterministic template-generated data versus cloud-generated natural-language data in the C5 recipe? |
| C13 | What held-out split axes are required to prove D-domain C5 gains are not memorization or lineage leakage? |
| C14 | What should the 50/100/150 mid-training C6 gate sample, and what thresholds should trigger continue, human pause, early stop, or blocked? |
| C15 | Is the training stack spike a pre-propose, propose-task, or apply-task gate, and what physical evidence must it produce before full C5 training? |
| C16 | Which training recipe fields are frozen, which are variable, and what evidence allows rank, scale, LR, backend, or masking changes to reopen? |
| C17 | How should the old base 10/23 anchor coexist with a new D-domain base recalibration anchor, and which one governs candidate comparison? |
| C18 | What exact scoring layers, denominators, thresholds, and fail priority should define the rebuilt C6 four-layer bench? |
| C19 | When should endpoint parity start relative to C5 retraining and C6 rebuild, and what is the minimum endpoint evidence before any endpoint-ready claim? |
| C20 | Without endpoint GBNF, what parser, repair, whitelist, and failure-enum policy should govern D-domain tool calls? |
| C21 | Is `tool -> IR -> state_cell -> card -> patch` a UIUE artifact, a mainline contract artifact, or both, and where should the ownership boundary be drawn? |
| C22 | What are the entry conditions for `demo-golden-run`, and which identifiers must be stable before UIUE can consume it? |
| C23 | Which decision types require mandatory ground-truth subagent or cross-vendor review, and what output schema should such review produce? |
| C24 | What status vocabulary should prevent `train_health`, data readiness, G6-C diagnostic, C6 model-quality, endpoint candidate, demo-golden, V-PASS, S-PASS, and U-PASS from being conflated? |
