## 1. Carrier and ownership gates

- [x] 1.1 In a dedicated clean worktree, validate this change with `openspec validate add-c1-demo-capability-governance --strict` and `openspec validate --all --strict`; record the exact base SHA and command outputs. **Superpowers: verification-before-completion.**
- [x] 1.2 Add a mechanical ownership audit that fails if governance owns presentation fields, execution owns customer copy, the bridge owns matrix eligibility, either MODIFIED delta is absent, or any `runtime-presentation-payload` capability appears. Write failing fixtures first, then the checker. **Superpowers: test-driven-development.**
- [x] 1.3 Add a 38-ID coverage assertion for `CG-002,004,005,007,008,009,014,015,019,022,023,024,025,026,027,028,036,038,039,041,044,045,048,049,050,053,054,055,057,058,059,060,063,065,068,074,076,080`; fail on missing, duplicate or extra P1/D0G IDs. **Superpowers: test-driven-development.**
- [ ] 1.4 Before each implementation slice, create an isolated worktree from the recorded implementation base, list owned/no-touch paths, and keep all commits limited to that slice; never use `git add .`. **Residual (evidence-chain):** only `Reports/B1-slice-receipt.json:2-16` and `runs/b3a-p1-fix/RECEIPT-B3A-P1FIX.md:1-23` survive; per-slice worktree/owned/no-touch evidence is incomplete and must not be reconstructed retroactively.

## 2. Matrix source, schema and checker

- [x] 2.1 Write failing checker fixtures for missing same-cell basis, unknown `primary_class`, duplicate identity, FastPath-only `actionDemoProven=true`, dropped no-representative cell and free-string reason; then add `contracts/demo-capability-matrix.json`, its schema and checker. **Reconciled historical implementation:** `scripts/test_check_capability_matrix.py:184-214`; `Tools/checks/check_capability_matrix.py:590-710`; commits `ac6c3b90`, `fb1df44d`.
- [x] 2.2 Make the checker preserve exactly 120 cells, recompute the ratified class counts and derive `actionDemoProven` only from mounted/semantic/state-cell/local-runtime-readback evidence; emit a conflict receipt instead of a hand-edited green exception. **Reconciled historical implementation:** `Tools/checks/check_capability_matrix.py:625-633,661-710,719-783`; commits `ac6c3b90`, `22d60ce2`, `fb1df44d`.
- [x] 2.3 Write failing deterministic-generation tests before adding the Swift matrix catalog; assert 120 cells, source digest, closed enums, conditional-lane separation and byte-identical regeneration. **Reconciled historical implementation:** `Tools/generate_demo_capability_matrix_swift.py:95-154`; `Tests/MAformacCoreTests/DemoCapabilityMatrixGeneratedTests.swift:6-21,78-113`; commit `6169e909`.
- [ ] 2.4 Run task-specific tests, GitNexus `detect_changes` against the slice base, and commit only matrix-owned files with a receipt containing base/head SHA, touched paths, commands and proof class. **Residual (evidence-chain):** `Reports/FIX-round7-receipt.md:3-8,27-38` records final gates but not the matrix slice's exact `detect_changes`; no merge/final-green inference is permitted.

## 3. Fallback catalog and enum projection

- [x] 3.1 Write failing tests for missing/duplicate 10-family×4-class pairs, unknown/free-string enums, raw `finiteReason` in customer copy, missing dialog/TTS, generic leakage and generated-copy drift; then add the authoritative fallback source, schema, checker and generated catalog. **Reconciled historical implementation:** `scripts/test_check_fallback_scripts.py:76-114`; `Tests/MAformacCoreTests/FallbackScriptCatalogTests.swift:5-44`; commit `285113d9`.
- [x] 3.2 Assert the locked internal reason → `fallback_reason` → safe `reasonKind` mapping, including `fast_path_no_match -> unsupported_no_available_tool`, attributable unmounted `name_rejected`, typed safety gap and clarification-not-refusal behavior. **Reconciled historical implementation:** `scripts/test_check_fallback_scripts.py:157-177`; `Tests/MAformacCoreTests/FallbackScriptCatalogTests.swift:23-44`; `design.md:62-74`; commits `a3fcaf3e`, `7bd10cb5`.
- [x] 3.3 Keep UI badge labels separate from authoritative dialog/TTS and report in-scope execution pass rate separately from out-of-scope fallback quality/generic leakage. **Reconciled historical implementation:** `Tools/checks/check_c1_anchor_delta.py:294-315,370-382`; `Tools/checks/check_fallback_scripts.py:590-626`; commit `f00a6730`.
- [x] 3.4 Run task-specific tests, deterministic regeneration, GitNexus `detect_changes`, and commit only fallback-owned files with a source/generated digest receipt. **Reconciled historical implementation:** `Reports/B1-slice-receipt.json:2-16`; `Tools/checks/check_fallback_scripts.py:590-605`; commits `4d69bb3e`, `a7b914fd`.

## 4. Router ingress and bounded CG-036 execution

- [x] 4.1 Before editing any existing function/class/method, run GitNexus impact and write a receipt; HIGH/CRITICAL runner or trace surfaces require exact commander risk acknowledgement before edits. **Reconciled historical implementation:** `runs/b3a-p1-fix/RECEIPT-B3A-P1FIX.md:16-23`; commits `d8828911`, `fb2e118d`.
- [x] 4.2 Write a failing router test proving two frames survive in order and zero frames remain fail-closed; then add a plan-level ingress while preserving any required single-frame compatibility wrapper. **Reconciled historical implementation:** `Core/Execution/DemoRuntimeSessionRunner.swift:75-115`; `Tests/MAformacCoreTests/DemoRuntimeSessionRunnerPartialExecutionTests.swift:343-430`; commit `eeed2734`.
- [x] 4.3 Write failing execution tests for accepted+unmounted, accepted+safety-denied, refused-only no-mutation, stale/unknown/length failure and unreviewed-extra-action rejection; then implement the smallest bounded multi-intent seam. **Reconciled historical implementation:** `Tests/MAformacCoreTests/DemoRuntimeSessionRunnerPartialExecutionTests.swift:68-97,218-270,343-430`; commits `70d3b850`, `b0bfdbbd`.
- [x] 4.4 Write failing trace tests for item identity, accepted readback, refused internal reason, observed calls, canonical before/after state and one-turn correlation; then extend only internal execution trace facts. **Reconciled historical implementation:** `Tests/MAformacCoreTests/RuntimeTraceFallbackTests.swift:5-52,87-117`; commit `e501c313`.
- [ ] 4.5 Run targeted router/runner/trace tests, GitNexus `detect_changes`, and commit each LOW/HIGH slice separately with touched-symbol and residual-risk receipts. **Residual (evidence-chain):** router/partial/trace commits `eeed2734` / `70d3b850` / `e501c313` exist, but only B3a has `RECEIPT-B3A-P1FIX.md:1-23`; separate touched-symbol, residual-risk and detect receipts are absent.

## 5. Existing bridge projection only

- [ ] 5.1 Before editing bridge symbols, run GitNexus impact on the partial adapter and public payload types; if existing schema cannot express the required projection, stop and create an explicit versioned migration decision rather than adding an ad-hoc field. **Residual (historical-pre-edit evidence):** `Core/Presentation/RuntimePresentationBridge.swift:759-848` proves the public surface exists, but no dedicated edit-before-impact receipt survives; do not backfill that historical claim.
- [x] 5.2 Write failing bridge/fixture tests for missing accepted readback, lost refused identity, wrong safe reason, raw internal reason leakage, unknown top-level field and proof-cap upgrade; then project execution facts through existing bridge-owned fields. **Reconciled historical implementation:** `Core/Presentation/RuntimePresentationBridge.swift:759-848`; `Tests/MAformacCoreTests/RuntimePresentationBridgeTests.swift:134-277`; commits `b3fd0c93`, `df4278c6`.
- [x] 5.3 Preserve main-owned schema/version, customer-safe `reasonKind`, cards/readbacks, private-marker redaction and UIUE consumer fail-closed behavior; do not create any C1-owned payload field. **Reconciled historical implementation:** `Core/Presentation/RuntimePresentationBridge.swift:759-768,941-951`; `Tests/MAformacCoreTests/RuntimePresentationBridgeTests.swift:245-277`; commits `b3fd0c93`, `7bd10cb5`.
- [ ] 5.4 Run bridge/public-fixture/consumer tests, record fixture digests, run GitNexus `detect_changes`, and commit only bridge-owned files. **Residual (evidence-chain):** bridge commit `b3fd0c93` covers bridge/fixture/test files, but no bridge-only fixture digest + `detect_changes` + receipt is retained.

## 6. Fallback probes and no-action proof

- [x] 6.1 Write failing fixtures for missing/duplicate family-reason pairs, forced state delta, forced tool call count 1, missing trace, safe-copy mismatch and raw-reason leakage; then define 40 probes referencing the authoritative fallback catalog. **Reconciled historical implementation:** `Tests/MAformacCoreTests/RuntimeNoMutationProbeTests.swift:9-59`; `scripts/test_check_runtime_no_mutation_receipts.py:82-145`; commits `0016d3e9`, `5c501384`.
- [x] 6.2 Generate receipts from observed canonical before/after mock state and tool calls; require `case_count=40`, complete pair coverage, zero calls and no state mutation for pure fallback/refusal/clarify paths. **Reconciled historical implementation:** `verification/w3-w2-audit/receipts/c1/runtime-no-mutation-40-probes.json:1-18`; `Tools/checks/check_c1_anchor_delta.py:258-315`; commit `5c501384`.
- [ ] 6.3 Run probe tests and checker, capture the durable run-dir receipt, run GitNexus `detect_changes`, and commit only probe-owned files. **Residual (evidence-chain):** `runtime-no-mutation-40-probes.json:1-18` and `REAUDIT-round7.md:47-54` prove the receipt/probes, but not the probe-owned commit's exact `detect_changes`.

## 7. S10 prelay, mounted no-delta and rollback guards

- [x] 7.1 Write failing S10 checker fixtures for missing joint rate, stale run identity, prose-only rate and wrong min formula; then enforce `joint=min(hedged,can_question)` without claiming S10 ran. **Reconciled historical implementation:** `scripts/check_s10_receipt.py:167-255`; commit `3a82acfa`; fixture-only and `s10_executed=false` remain explicit.
- [x] 7.2 Write failing mounted-policy fixtures for growth without matrix evidence, `actionDemoProven` growth without S10/owner gates, missing golden/readback and rollback that drops fallback; then implement the guard. **Reconciled historical implementation:** `scripts/check_mounted_catalog_no_delta.py:95-119,175-230`; `scripts/test_check_mounted_catalog_no_delta.py:123-157`; commits `f2469776`, `d74ce25f`.
- [x] 7.3 Compare mounted authority at implementation base and head and require zero delta for C1; keep matrix/fallback/probe prelay allowed and record CG-080 non-claims. **Reconciled historical implementation:** `REAUDIT-round7.md:99-111`; commits `f2469776`, `d74ce25f`; mounted 1→N remains unauthorized.

## 8. CI, anchor comparison and closeout

- [x] 8.1 Write failing tests for generic/dynamic CI receipt identity, then wire governance, matrix, fallback, probe, S10 and mounted gates into source-free verification without hardcoding one change ID. **Reconciled historical implementation:** `Makefile:38-58,81-146`; `.github/workflows/verify.yml:31-54`; commits `b552a1d5`, `92ffd8fa`.
- [x] 8.2 Add a durable anchor checker that compares base/head matrix counts and digests, mounted delta, 40-pair coverage, partial producer/consumer evidence and separate execution/fallback metrics; reject stale base or proof-class upgrade. **Reconciled historical implementation:** `Tools/checks/check_c1_anchor_delta.py:146-315,370-390`; commit `f00a6730`.
- [ ] 8.3 Integrate slice commits in dependency order and run `openspec validate --all --strict`, targeted tests, `swift test`, `make verify-all`, the MAformacMac Xcode build, `make verify-ci`, `git diff --check`, and GitNexus `detect_changes`. **Residual (evidence-chain):** `REAUDIT-round7.md:69-77` proves final gates, but the integrated PR-head `detect_changes` receipt is not retained; fresh OpenSpec validation is not a substitute.
- [x] 8.4 Have an independent auditor rerun authority, 38/38 coverage, partial execution+bridge, 40 probes, mounted no-delta, CI and final gates; only P0/P1=0 may close this change. **Reconciled historical implementation:** `REAUDIT-round7.md:1-17,19-77,99-123`; audited head `e360213c`, merged by `f5ef6270`.
- [x] 8.5 Keep closeout proof classes separate and retain non-claims: no mounted 1→N, S9/S10 execution, C5/C6 acceptance, candidate, mobile, true-device, live API, operator-pass or V-PASS. **Reconciled historical implementation:** `REAUDIT-round7.md:15-17,113-117`; `docs/commander-log/decisions.md:1306-1309`; PR #42 merge `f5ef6270`.
## int-v5a Commit B tasks

- Generate the exact-composite runtime bundle manifest and Swift catalog.
- Switch the default runner to the generated catalog; retain no inline production payload.
- Emit and validate the single v2 action receipt with bundle digest and nullable v5c catalog digest.
- Keep v5b/v5c frontstage, witness, and readiness carriers read-only.
