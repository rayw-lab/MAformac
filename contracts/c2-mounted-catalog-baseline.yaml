# CG-080 Mounted Catalog Baseline & Rollback Guard
# This file is the SSOT for the frozen mounted catalog baseline.
# Do not edit this file manually - it is enforced by check_mounted_catalog_no_delta.py.

baseline:
  mounted_tool_names:
    - "adjust_ac_temperature_to_number"
  catalog_sha: "616d4dbc07f21f1599a373af8aaf1d3152df05c5956ef61358c4645cc3a53fa5"
  description: "C2 frozen baseline: mounted catalog 1→N forbidden, only adjust_ac_temperature_to_number allowed"

rollback_guard:
  fallback_mounted_tool_names:
    - "adjust_ac_temperature_to_number"
  can_demo_fallback: true
  fallback_description: "Rollback to safe C1 baseline if C2 delta detected"

enforcement:
  check_script: "scripts/check_mounted_catalog_no_delta.py"
  make_target: "verify-mounted-catalog-no-delta"
  integrated_in: ["verify", "verify-all"]
