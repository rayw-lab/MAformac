# CG-080 Mounted Catalog Baseline & Rollback Guard
# This file is the SSOT for the frozen mounted catalog baseline.
# Do not edit this file manually - it is enforced by check_mounted_catalog_no_delta.py.

baseline:
  mounted_tool_names:
    - "adjust_ac_temperature_to_number"
    - "close_ac"
    - "open_atmosphere_lamp"
    - "open_seat_heat"
    - "open_window_by_number"
  catalog_sha: "b7ab19939cb5821962f706fe8f2b50524ac85acc1217599d14e10d14db0e5794"
  description: "pre-G0 candidate mount (AF-G0 still RED): AC pair + window/ambient/seat candidates; actionDemoProven stays 0/120; 禁演示/合并"

rollback_guard:
  affected_action_demo_proven_after_rollback: false
  preserve_artifacts:
    - "fallback_catalog"
    - "fallback_probes"
  evidence_contract: "Pass --rollback-state JSON with before/after mounted names, affected cell before/after actionDemoProven values, and before/after fallback artifact paths"

enforcement:
  check_script: "scripts/check_mounted_catalog_no_delta.py"
  make_target: "verify-mounted-catalog-no-delta"
  integrated_in: ["verify", "verify-all"]
