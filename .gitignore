# 开源参考代码(1.2G,外部代码不进主仓;保留 reports/snapshots 作研究证据)
referencerepo/repos/

# openspec 工作区数据(specs/changes 进 git,但临时态除外)
# (openspec init 后再细化)

# macOS
.DS_Store

# Xcode / Swift
build/
.build/
DerivedData/
*.xcresult
*.hmap
*.ipa
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

# 临时
*.tmp

# 模型 / 权重 / 训练与真实座舱资料
models/
weights/
checkpoints/
datasets/
training-data/
raw-data/
vehicle-raw/
seat-cockpit-raw/
*.safetensors
*.gguf
*.mlx
*.mlpackage
*.mlmodelc
*.ckpt
*.pt
*.pth
*.onnx
*.parquet
*.sqlite
*.db

# Python 开发期环境(不得进 iOS)
.venv/
venv/
__pycache__/
*.pyc

# harness-enforce 项目级试错 hook 不入仓（CVE-2025-59536/2026-21852：clone 时恶意 hook RCE）
# 代码在用户级 ~/.claude/scripts/；项目仓只放数据/契约/脚本，不放 hook 注册
.claude/hooks/
.claude/settings.local.json
.claude/settings.local.json.bak-*

# 脏区清理 2026-06-23: θ-α 失败run 2.8G + 工具临时(不入仓; 失败锚点 0/23 已在 grill-decisions 文档)
# Reports/* 忽略所有 runtime 报告, 仅保 .gitkeep(SwiftPM exclude 解析, 防 clean checkout Invalid Exclude warning)
Reports/*
!Reports/.gitkeep
.playwright-mcp/
.dispatch/
.cc-connect/

# Commander run nest pointer: keep only the hand-written README in git.
runs/*
!runs/README.md

# machine-specific symlinks to local Codex plugin cache (not project source)
Tools/agent-platform-plugin-refs/
# generated AI reference images (42M, regenerable from gptimage2-anchor-set/*.md prompts; kept local)
docs/design/anchors/

# 脏区清理 2026-06-26: UIUE Phase2 视觉迭代死循环产物(v1→v72 截图 + zone-compare, 687M 不入仓;
# 反模式物证已在 grill-tournament/uiue-visual-gate-harden-grill-decisions.md; 保留 anchor + zone-compare-phase6 的 README/metrics)
docs/research/2026-06-25-a2-execution/shots/
docs/research/2026-06-25-a2-execution/zone-compare-v*/
docs/research/2026-06-25-a2-execution/app-state-phase5-ambient/
docs/research/2026-06-25-a2-execution/*-sidebyside.png
